# Network+ AI Quiz — Project Guide

## Context
- **Single-user app currently** (the user is the student prepping for real N10-009). Explains why items like per-user API cost telemetry are deferred.
- **Future pivot**: paid multi-cert SaaS. Anything tagged `saas-gated` on either board is **frozen** until that trigger fires (see Conventions).
- **Origin**: started as a Notion-native flashcard quiz (2026-03-28), pivoted to standalone web app on 2026-04-02. **Notion sync is not on the roadmap** — do not re-propose.
- **External reviewer**: user periodically pastes OpenAI Codex feedback as a product-review signal — drove v4.40.0 label clarity and v4.41.0 homepage density.

## Architecture
- **Type**: Static HTML/JS/CSS single-page app, no framework, no build step
- **AI**: Three-way Claude model split, direct browser fetch with `anthropic-dangerous-direct-browser-access: true`. User supplies their own API key via Settings (stored under `STORAGE.KEY`).

  | Model | Constant | Used for |
  |---|---|---|
  | Haiku 4.5 (`claude-haiku-4-5-20251001`) | `CLAUDE_MODEL` | Bulk generation: `fetchQuestions`, `tbGenerateAiTopology` |
  | Sonnet 4.6 (`claude-sonnet-4-6`) | `CLAUDE_VALIDATOR_MODEL` | Semantic second-pass: `aiValidateQuestions` |
  | Sonnet 4.6 | `CLAUDE_TEACHER_MODEL` | Authoritative teacher content (Tier A/B/C — see Key Patterns) |

- **Storage**: All data in localStorage via the `STORAGE.*` namespace object (38 keys — see below)
- **Hosting**: Vercel static deployment, prod URL https://networkplus-quiz-sable.vercel.app
- **Offline**: Service worker with stale-while-revalidate caching. **Cache name MUST bump every deploy** — use `bump-version.js`, never hand-edit partial.
- **PWA**: `manifest.json`, installable on mobile

## Files
| File | Purpose | Size |
|---|---|---|
| `index.html` | All page structures (30+ pages: setup, quiz, exam, results, review, subnet, ports, drills launcher, topology, analytics, progress, guided labs, …) | ~113 KB / 1,907 lines |
| `app.js` | All app logic — state, AI calls, rendering, game loops, analytics, 5 activity sub-systems | **~1.8 MB / 31,738 lines** |
| `styles.css` | Full dark/light theme styling + `@media (prefers-reduced-motion)` gate | ~480 KB / 11,661 lines |
| `sw.js` | Service worker (stale-while-revalidate, shell-asset precache, 60-entry LRU cap) | 73 lines |
| `manifest.json` | PWA manifest | 646 B |
| `vercel.json` | Vercel config (minimal) | 806 B |
| `tests/uat.js` | UAT suite — **3,887 assertions** as of v4.62.5, embeds validation-audit gate | 8,677 lines |
| `tests/tech-debt.js` | CI thresholds: long-function count, LOC, global count, etc. | 131 lines |
| `tests/validation-audit.js` | 23-Q broken-corpus regression fixture (60% catch floor, 0 FP ceiling) | 589 lines |
| `tests/deploy-verify.js` | Post-deploy smoke against prod | 335 lines |
| `tests/e2e/app.spec.js` | Playwright E2E | — |

**Reality check:** `app.js` size is the driver for [#138](https://github.com/oremosu98/networkplus-quiz/issues/138) (module split) — `saas-gated`, don't start without pivot trigger. `styles.css` growth drove [#55](https://github.com/oremosu98/networkplus-quiz/issues/55) — same gate.

## Key Patterns

### Page Navigation
Pages are `<div class="page">` elements. `showPage(name)` toggles `.active`. All pages live in `index.html`.

### Quiz Flow
`startQuiz()` → `fetchQuestions()` (Haiku) → `aiValidateQuestions()` (Sonnet) → `validateQuestions()` (programmatic) → `injectPBQs()` → `render()`

### Question Types
| Type | Format | Scoring |
|---|---|---|
| `mcq` | 4 options, 1 correct | `pick()` |
| `multi-select` | 5 options, 2–3 correct | `submitMultiSelect()` |
| `order` | 4–5 items, arrange in sequence | `submitOrder()` |
| `cli-sim` | Terminal UI + command buttons + diagnosis MCQ | `pick()` for the MCQ part |
| `topology` | Device palette + zone placement | `submitTopology()` |

### Exam Mode
`EXAM_QUESTION_COUNT = 90`, 5 batches of 18, 90-min timer, scaled 100–900 (`EXAM_PASS_SCORE = 720`, `EXAM_MAX_SCORE = 900`). Uses `examQuestions[]` + `examAnswers[]`. Flag/unflag, navigator grid. **Strict Mode** (`STORAGE.HARDCORE_EXAM`) hides flag + prev + navigator for realistic simulation.

### AI Teacher Tiers
Every Claude call site is tiered. Tier determines model, caching, and ground-truth injection.

| Tier | Purpose | Sites | Model | Cache | GT injection |
|---|---|---|---|---|---|
| **A** | Authoritative study content (user trusts as truth) | `explainFurther()`, `showTopicDeepDive()` / `buildTopicDivePrompt()`, `fetchTopicBrief()` | Sonnet | `_aiCacheGet`/`Set`, djb2 hash, 20-entry LRU via `STORAGE.AI_CACHE` | `_buildGtHint(text, topicName)` prepends AUTHORITATIVE FACTS block |
| **B** | Math-must-be-exact coach | `stAskCoach()` (subnet), `ptAskCoach()` (port drill) | Sonnet | None | Deterministic facts computed + stuffed into prompt (IP binary breakdown, proto→port) |
| **C** | Topology coach | `tbCoachTopology()`, `tbExplainDevice()` | Sonnet | `STORAGE.TB_COACH_CACHE` (lab+step keyed) | None |
| **Gen** | Bulk generation (cost-optimized) | `fetchQuestions()`, `tbGenerateAiTopology()` | Haiku | None | Via `aiValidateQuestions()` → `validateQuestions()` post-pass |

### Ground Truth Tables
Static N10-009 facts. **Dual-layer pattern**: used both for prompt injection (Tier A) AND programmatic reject in `validateQuestions()`.

| Table | Entries | Covers |
|---|---|---|
| `GT_PORTS` | 27 | protocol → port number |
| `GT_OSI` | 30 | protocol → OSI layer |
| `GT_WIFI_BROKEN` | 1 | `['wep']` — broken auth |
| `GT_WIFI_DEPRECATED` | 1 | `['wpa']` — original WPA |
| `GT_ETHERNET` | 6 | auto-negotiation, auto-MDIX, MDI/MDIX, crossover/straight-through, duplex, PoE 802.3af/at/bt |

- `_buildGtHint(text, topicName)` — scans question/topic for protocol keywords, prepends AUTHORITATIVE FACTS block to Tier A prompts.
- `_groundTruthOk(q)` — programmatic guard called from `validateQuestions()`. Rejects questions with deterministically wrong answers before they reach the quiz UI.
- **Adding a new fact**: update the GT table AND extend the corresponding regex in both `_buildGtHint` and `_groundTruthOk`. Both layers must know.

### Curated Exemplar Bank (v4.58–4.59 — the quality engine)

`QUESTION_EXEMPLARS` is a const array at the top of `app.js` holding **200 hand-curated MCQ exemplars** spanning all 5 CompTIA N10-009 domains. It is **NOT the exam question pool** — it's a **few-shot style reference** that gets injected into Haiku's system prompt every time a question is generated.

**Final distribution (v4.59.6 — aligned to CompTIA blueprint weights):**

| Domain | Count | Weight |
|---|---|---|
| 1.0 Networking Concepts | 46 | 23% |
| 2.0 Network Implementation | 40 | 20% |
| 3.0 Network Operations | 38 | 19% |
| 4.0 Network Security | 28 | 14% |
| 5.0 Network Troubleshooting | 48 | 24% |
| **Total** | **200** | **100%** |

**Injection mechanism (in `_fetchQuestionsBatch`):**
1. `_pickExemplarsForTopic(qTopic, 3)` — tiered selection: exact topic match → same-domain fallback (via `TOPIC_DOMAINS`) → any exemplar. Strips `"Multi: "` sentinel. Caps at 5. Returns `[]` if bank empty (no-op).
2. `_formatExemplarsForPrompt(exemplars)` — wraps in `QUALITY REFERENCE` block framed as **"style references only — DO NOT copy these exemplars"**.
3. Injected into the generation prompt after the Difficulty line.

**3 exemplars per batch**. A 90-Q exam = 5 batches × 3 = max 15 exemplar-slots per run. With 200 in the pool, same 3 rarely appear twice in one session.

**Scope (everything that calls `fetchQuestions`):** Custom Quiz, Exam Simulator (all 5 batches), Daily Challenge, Marathon Mode, Topic Brief generation, Follow-up drill on wrong answers. Topology Builder does NOT use the bank — it generates diagrams, not questions.

**Legal boundary (invariant)**: every exemplar is original content authored from the public N10-009 blueprint. Zero paid-bank (Jason Dion / CertMaster / Myers / Kaplan) content ingested. Not stems, not options, not explanations. Documented in git history for clean IP audit trail.

**Jason Dion Method (recalibration loop)**: when user takes a legally-purchased practice test, they share gap topics **in their own words only** → Claude authors 3–5 new exemplars per gap → ships as Phase 3 Cycle N → user studies → concepts get appended to `RETENTION_GAP_CONCEPTS` (v4.62.3) so they surface in every future quiz + exam generation as a soft tiebreaker. Quarterly cadence, ~3 hrs per cycle. Never ingest paid content. See memory files `reference_jason_dion_method.md`, `reference_retention_concepts_pattern.md`, and `feedback_concept_mockup_first.md` for the full process + related workflow patterns.

**Cert-agnostic**: the pattern scales across certs. Security+ / CCNA / AWS SAA / Azure AZ-104 each get their own `QUESTION_EXEMPLARS_<CERT>` array + topic whitelist + objective regex. The 7-layer validation stack, prompt structure, batching, Sonnet escalation — NONE of it changes per cert, only content. This is what makes multi-cert SaaS scalable. See `cert_saas_pivot_plan.md`.

### Validation Pipeline (7 layers — hardened v4.57–4.59)
1. **Prompt self-verification** — 5-step check baked into the `fetchQuestions` system prompt
2. **Curated exemplar bank** (v4.58.0 infrastructure, v4.59.6 200-exemplar content) — few-shot quality reference injected per batch
3. **AI second-pass** — `aiValidateQuestions()` on Sonnet. 6 conceptual checks (v4.57.0 expansion): factual correctness, premise↔answer consistency, explanation match, **conceptual coherence** (stem asks about X but answer tests Y), **framing match** (abstraction level alignment), **distractor quality** (≥2 plausible tempters)
4. **Interrogative-stem gate** (v4.57.2) — `_stemHasInterrogative()` rejects stems missing "?" or interrogative words (which/what/when/etc.)
5. **Programmatic validator** — `validateQuestions()`:
   - Unique-word scoring with negation guard (skips on NOT/EXCEPT/"which not")
   - `_groundTruthOk(q)` against GT tables (27 ports, 30 OSI, WiFi broken/deprecated, Ethernet facts)
   - `_numericOptionOk(q)` for all-numeric options
   - `_smallestSubnetOk(q)` — v4.43.8 smallest-satisfying-subnet rule
   - Reported-question exclusion (`STORAGE.REPORTS`)
6. **Regression gate** — `tests/validation-audit.js` runs a 23-Q broken-corpus fixture on every UAT run. `MIN_CATCH_RATE = 60`, `MAX_FP_RATE = 0`. Current: **64.7%** catch / **0%** FP. **Do NOT tune `validateQuestions()` without running the audit first.**
7. **Sonnet escalation tier** (v4.56.2) — when both Haiku attempts (full + stripped prompt) fail on a batch, retry on Sonnet before surfacing error to user. Silent on success. Batching coordinator (v4.56.1) keeps per-batch prompts in Haiku's comfort zone.

**Exam-mode parity (v4.43.5):** `startExam()` runs the SAME 4-layer pipeline per batch of 18, with `EXAM_BATCH_BUFFER = 5` over-request + retry-to-fill + graceful degradation banner if the final exam ships with <90 questions. Pre-v4.43.5 exam mode shipped raw Haiku output with zero validation — ~9–36 of 90 questions could carry quality issues into the real simulator.

**Quiz-count guarantee (v4.43.4):** `startQuiz()` uses `DROPOUT_BUFFER = Math.max(3, Math.ceil(qCount * 0.3))` over-request + retry-to-fill so `questions.length === qCount` always (unless two consecutive generations drop heavily, in which case ship what we have). Fixes the "requested 10, got 7" bug where the old pipeline silently accepted shortfalls ≥ qCount/2.

### Data Storage (localStorage)
All keys centralized in the `STORAGE` namespace at `app.js:64–103`. 38 keys, all prefixed `nplus_`:

| Category | Keys |
|---|---|
| Core | `KEY`, `THEME`, `HISTORY`, `STREAK`, `WRONG_BANK`, `REPORTS`, `ERROR_LOG` |
| Exam | `HARDCORE_EXAM`, `EXAM_DATE` |
| Ports | `PORT_BEST`, `PORT_STREAK_BEST`, `PORT_FAMILY_BEST`, `PORT_PAIRS_BEST`, `PORT_STATS`, `PORT_MASTERY`, `PORT_LESSONS` |
| Subnet | `SUBNET_STATS`, `SUBNET_MASTERY`, `SUBNET_LESSONS` |
| Other drills | `AB_MASTERY`/`AB_LESSONS` (Acronym Blitz), `OS_MASTERY`/`OS_LESSONS` (OSI Sorter), `CB_MASTERY`/`CB_LESSONS` (Cable ID) |
| Topology | `TOPOLOGIES`, `TOPOLOGY_DRAFT`, `TB_COACH_CACHE`, `LAB_COMPLETIONS`, `FIX_CHALLENGES` |
| Goals + analytics | `TYPE_STATS`, `DAILY_GOAL`, `DAILY_CHALLENGE`, `MILESTONES`, `DEEP_DIVE_USES` |
| AI | `AI_CACHE` (Tier A LRU) |
| Monitoring | `GH_TOKEN`, `GH_REPORTED` |

**Convention**: always access through `STORAGE.*`; never inline `localStorage.getItem('nplus_…')`. Export/Import Data (under Settings) walks the namespace.

### Weak Spots v2 — `computeWeakSpotScores()`
At `app.js:3672`. Drives the homepage `🎯 Weak spots` chip row + Subnet Trainer dashboard callouts. 4 combined signals:

1. **Recency-decayed wrong-bank count** — exp decay with 7-day half-life (`WEAK_HALF_LIFE_WRONGS_MS`), `diffWeight()`-weighted (Hard 2.0× / Exam 1.5× / Found 1.0×), half-credit when `rightCount >= 1` (partial graduation)
2. **Bayesian posterior accuracy gap** — recency-decayed weighted correct/total (14-day half-life via `WEAK_HALF_LIFE_HIST_MS`, 1.3× exam boost), Beta(2,2) prior: `posterior = (wCorrect + 2) / (wTotal + 4)`. Gap = `max(0, WEAK_TARGET_ACC - posterior)` where `WEAK_TARGET_ACC = 0.85`
3. **Staleness** — after `WEAK_STALENESS_DAYS = 14` untouched, up to `WEAK_STALENESS_CAP = 2.0` multiplier
4. **Domain importance** — `_weakDomainMultiplier(topic)` = `DOMAIN_WEIGHTS[dom] / WEAK_AVG_DOMAIN_WEIGHT` (0.7× security ↔ 1.2× troubleshooting)

Final score = `(wrongsRecent * 3.0 + accGap * 25.0 + staleness * 2.0) * domainMul`. **Low-signal filter** excludes `wTotal < 1 && wrongsRecent < 0.5` (untouched, not weak). `renderTodaysFocus()` is refreshed in real time via hooks in `finish()`/`submitExam()`.

### Readiness Score — `getReadinessScore()`
At `app.js:3066`. Composite 420–870 scaled score.
- **Signals (weighted 40 / 25 / 20 / 15)**: accuracy / coverage / recency / volume
- **Accuracy** — within-domain question-count weighted (v4.42.4 fix — do NOT revert to `pctSum/count`), CompTIA domain weights 23/20/19/14/24 (`DOMAIN_WEIGHTS`), difficulty multipliers, exam-mode boost 1.3×
- **Coverage** — studied topics / total (50), × 0.5 penalty under 5 topics
- **Recency** — topics touched in last 14 days, 7-day boost 2×
- **Volume** — total Qs answered, capped at 500

Deferred tuning ([#139](https://github.com/oremosu98/networkplus-quiz/issues/139), `saas-gated`): recency denominator redundancy with coverage signal, no PBQ weighting, triple coverage-penalty. Revisit with real SaaS user data.

### Catalog & Domain Weights
- `TOPIC_DOMAINS` at `app.js:2979` — 50 topics → domain mapping (v4.42.3 expanded from 40)
- `DOMAIN_WEIGHTS` at `app.js:2950` — 5 N10-009 domains: Fundamentals 0.23 / Implementation 0.20 / Operations 0.19 / Security 0.14 / Troubleshooting 0.24
- `topicResources` — per-topic Professor Messer YouTube search URLs + N10-009 objective numbers
- **Convention**: when splitting a topic (e.g. `Routing Protocols` → add `OSPF`, `BGP`), **keep the parent umbrella** in all 3 surfaces (`TOPIC_DOMAINS`, `topicResources`, HTML chip list) so old history entries aren't orphaned. UAT locks this.

### Milestones
- `MILESTONE_DEFS` at `app.js:3234` — 47 entries with `{id, icon, label, desc, ...}`
- `MILESTONE_CHECKS` at `app.js:3392` — 47-entry declarative table: `{id, check: (ctx) => boolean}`
- `_buildMilestoneCtx()` — gathers context once (history, exam stats, readiness, streak, per-activity mastery, day/time bookkeeping) with defensive try/catch
- `evaluateMilestones()` — 10-line loop, calls `unlockMilestone(id)` on matching checks, returns newly-unlocked IDs
- **Hook**: called from `finish()` AND `submitExam()` (not lazy — was a latent bug pre-v4.42.0 where milestones only unlocked if user opened Analytics). Newly-unlocked IDs fire `showMilestoneCelebration(id)` = toast + `launchMiniConfetti()`
- **Adding a milestone**: one entry in `MILESTONE_DEFS`, one in `MILESTONE_CHECKS`. UAT regression-guards all 47 IDs.

### Keyword Highlighting
14-word exam-trap dictionary at `app.js:19269` (`EXAM_KEYWORDS`): NOT / EXCEPT / CANNOT / MOST / LEAST / BEST / WORST / PRIMARY / FIRST / LAST / NEXT / ALWAYS / NEVER / ONLY. **Deliberately excludes**: all / none / any / could / should / would / which (too common in prose). `setQuestionText(el, raw)` is the **single entry point** for all 5 question-stem render paths. **XSS-critical order**: `escHtml(raw)` → `highlightExamKeywords(escaped)` → `el.innerHTML`. Never reverse.

### Animation Inventory
| Pattern | Helper / keyframe | Used for |
|---|---|---|
| Count-up | `animateCount(id, from, to, ms, suffix='')` | Readiness number, Daily Goal counters |
| Rainbow confetti | `launchConfetti()` (150 particles, 8 colors) | Exam pass |
| Gold mini-confetti | `launchMiniConfetti()` (40 particles, radial) | Milestone unlock — subtler celebration |
| Celebration toast | `showCelebrationToast(title, sub)` → `showMilestoneCelebration(id)` | Milestone toasts, purple/gold gradient |
| Streak pulse | `_pendingStreakPulse` flag set in `finish()`/`submitExam()`, consumed in `renderStreakBadge()` | `@keyframes streakPulse` scale 1→1.12→1 + orange ripple |
| FLIP rerank | `renderTodaysFocus()` captures `oldRects` map keyed by `data-topic`, computes dx/dy, applies inverse transform, releases w/ `transition: transform 420ms cubic-bezier` | Weak-spots reranking after a quiz |
| Intro fill | `_readinessIntroArmed` / `_dailyGoalIntroArmed` module flags, once-per-session | Landing-page reveal |
| Answer-pick glow (v4.44.0) | `@keyframes optGlowPulse` — layered on `optBounce` via comma-anim; 0.9s cubic-bezier, 0.25s delay so it fires after the bounce settles | `.option.correct` — green `box-shadow` ripple 0 → 16px → 0 opacity |
| Question reveal + option stagger (v4.44.0) | `@keyframes qTextReveal` + `optionStaggerIn`; `render()` toggles class off→reflow→on, sets per-index `animationDelay = (i * 80) + 'ms'` | New-question pacing — stem fades up, options glide in 80ms apart |
| Block-match ✅ pop (v4.44.0) | `@keyframes stBlockMatchPop` with overshoot cubic-bezier; `_stSetupBlockMatchObserver()` uses `IntersectionObserver` (threshold 0.5, one-shot unobserve) to trigger when the lesson card scrolls into view | Subnet Lesson 4 Step 4 + Lesson 5 put-it-all-together visual — the ✅ on the matching block |
| Progress bar smooth fill (v4.44.0) | `.progress-fill` uses `transition: width .6s cubic-bezier(0.2, 0.8, 0.2, 1)` | Quiz/exam question-to-question progress |
| Domain Mastery bar fill (v4.45.0) | `.dm-bar-fill` uses `transition: width 800ms cubic-bezier(0.2, 0.8, 0.2, 1)` | Domain bars animate to target % on first render |
| Milestones progress bar (v4.45.2) | `.ana-ms-bar-fill` same cubic-bezier as Domain Mastery | Total unlocked % in Milestones header |
| Readiness bar smooth fill (v4.46.0) | `.ana-ready-bar-fill` uses `transition: width .8s cubic-bezier(0.2, 0.8, 0.2, 1)` | Exam Readiness hero score bar — smoother than old `.6s ease` after a quiz roll-up |
| Domain-row bar fill (v4.46.0) | `.ana-domain-fill` uses `transition: width .7s cubic-bezier(0.2, 0.8, 0.2, 1)` | Hero-card domain breakdown bars — matches Domain Mastery card aesthetic |
| Date-chip hover lift (v4.46.0) | `.ana-ready-datechip:hover` → `translateY(-1px)` + shadow bloom, `transition: all .2s ease` | Subtle interactive affordance on the merged date picker chip |
| Streak flame pulse (v4.46.1) | `@keyframes streakFlamePulse` — `scale(1) ↔ scale(1.1)` + `drop-shadow` bloom; 2.4s infinite on warm/hot tiers, 1.6s on blazing | `.ana-streak-flame` when tier ≥ warm (3+ day streak); reduced-motion gated |
| TB scenario Learn-more fade-in (v4.47.0) | `@keyframes tbScenarioLearnFade` — `opacity:0 → 1` + `translateY(-4px → 0)` 0.4s cubic-bezier; chevron rotates 90° on `[open]` via `transition: transform .25s cubic-bezier(0.2, 0.8, 0.2, 1)` | `.tb-scenario-learn-body` when user expands the `<details>` in scenario panel |
| TB scenario-loaded card fade-in (v4.47.2) | `@keyframes tbScLoadedIn` — `opacity:0 → 1` + `translateY(6px → 0)` 0.45s cubic-bezier | `.tb-sc-loaded` when rendered by `tbRenderEmptyHint()` as in-canvas scenario card |

**Accessibility gate**: all wrapped by `@media (prefers-reduced-motion: reduce)` in `styles.css` — transitions neutralized to `.01ms linear`, pulse animations killed, final state painted without motion.

**FLIP guard**: `finish()` / `submitExam()` do NOT call `renderTodaysFocus()` directly. Only `goSetup()` does — so FLIP gets a real old→new delta to animate. UAT regression-guards this (see Conventions).

### Topic Resources
`topicResources` object maps all 50 topics to Professor Messer N10-009 YouTube search URLs + objective numbers. Used by `showTopicDeepDive`, `getWeakTopic` fallback, and per-row Progress-page play buttons.

## Feature Sub-Architectures

### Topology Builder (~4,500 LOC in app.js)
Canvas: `TB_CANVAS_W = 1800`, `TB_CANVAS_H = 1100` (v4.39.0 bump for AI-generated topologies). Key pieces:
- `TB_LAB_CATEGORIES` at `app.js:13570` — groups the lab catalog by concept (Fundamentals / Switching / Routing / Services / Security / Cloud & WAN / Wireless & QoS / Troubleshooting). **29 concept labs + 35 auto-generated config variants** (honest count; previously inflated to "65 labs").
- `tbActiveLab` state drives Coach context: when a lab is active, `tbCoachTopology()` prompts Claude as tutor with step N of M + step goal + hint. Cache key differentiated by lab+step.
- `tbDeepValidateAndFix(state, scenario)` — AI-output guard, runs after every AI generation to catch malformed JSON, missing fields, bunched positioning.
- `tbAutoLayout(state)` — force-directed post-pass. Detects clustered seeds (spread <250px), grid-seeds, runs 260 iterations (`REPULSE = 9000/dist²`, `SPRING = 0.06`, `IDEAL_LINK = 200`, velocity damping 0.85, cap 30), final hard-separation pass to `MIN_SEP = 150`.
- `tbProcessCliCommand(dev, cmd)` — 8-line dispatcher over the declarative `_TB_CLI_COMMANDS` table (~37 entries). Each CLI command maps to an isolated `_cli<Name>(dev, cmd)` handler. Match spec: `string | string[] | (cmd)=>bool`. Refactored from a 452-line if/else chain in v4.62.4 / #126 — see *Regression-Guard Tombstones*.
- L2/L3 simulation engine, packet-flow animation, "Fix This Network" challenges, PNG export, AI Generate v2, AI Coach.
- **Scenarios (v4.47.0)**: `TB_SCENARIOS` array at `app.js:7358` holds **16 scenarios** (Free Build + 8 legacy + 7 new). Each has `{id, title, description, requirements, ruleIds, requires, explanation}`. The `explanation` field powers a collapsible "Learn more about this network" `<details>` panel below the requirements list — 5 sub-sections (Overview / How it routes data / Key devices / Key concepts / Exam relevance) keyed to N10-009 objectives. Grouped in the dropdown via `<optgroup>` (Campus & Enterprise / WAN Architectures / Cloud Networking). Light grading convention: new scenarios reuse `min-devices` + `no-orphans` + device-count requirements rather than authoring brand-new rule functions — educational value comes from the shape + explanation. `tbRenderScenarioPanel` at `app.js:7527` builds the UI conditionally on `scen.explanation` so a scenario without the field still renders cleanly (backward-compatible).
- **Consumer endpoint devices (v4.47.0)**: `laptop`, `smartphone`, `game-console`, `smart-tv` added to `TB_DEVICE_TYPES` + `TB_PALETTE_GROUPS.Endpoints` + `TB_IFACE_DEFAULTS`. All are single-interface client devices (smartphone uses `wlan0`, others `eth0`). The `isEndpoint` family check (3 sites: health check at `app.js:6661`, overlay at `app.js:8127`, gateway-UI at `app.js:8199`) was expanded to include all 4 — so they accept `gateway` config + render correct health color + show the gateway field in the overlay.

### Subnet Trainer
- 21 granular question types (CIDR, VLSM, usable hosts, broadcast, gateway placement, NAT boundary, …). Adaptive difficulty, step-by-step feedback.
- `stRenderDashboard()` shows 3 prescriptive callouts: **Weakest Categories** (<75% acc, ≥5 attempts), **Stale Categories** (>7 days), **Weakest Question Types** (<70% acc, ≥3 attempts). Each click-through jumps to `stDashJumpToCategory(catId)` = Practice tab + Focus mode + category pre-selected.
- `stAskCoach()` — Tier B. Computes deterministic binary breakdown (IP / mask / IP AND mask) and stuffs into prompt as AUTHORITATIVE FACTS before asking Sonnet. AI can't hallucinate the math.
- `WEAK_SPOT_DRILL_BRIDGES` at `app.js:3780` — main-app weak topics that route into Subnet Trainer (`Subnetting & IP Addressing`, `IPv6`, `NAT & IP Services`). Deduped. **Topology Builder deliberately excluded** — it's a standalone activity, not a reactive drill target.

### Drills Launcher
`#page-drills` wraps 4 drills consolidated from what was a 4-tile nav row (v4.41.0):
1. **Port Drill** — `startPortDrill()` — 4 modes (Timed / Endless / Family / Secure Pairs)
2. **Acronym Blitz** — `startAcronymBlitz()` — 130+ N10-009 acronyms
3. **OSI Sorter** — `startOsiSorter()` — drag/click protocols onto correct OSI layer
4. **Cable & Connector ID** — `startCableId()` — cable image → name

Shared `createDrillScaffold(cfg)` at `app.js:17766` syncs `.active` / `aria-selected` / `aria-pressed` states across tabs + modes. All 3 newer drills (AB/OS/CB) have full ARIA coverage matching quiz/exam pages post-v4.42.5. Each drill has its own `*_MASTERY` + `*_LESSONS` localStorage keys.

## Conventions

> **Before building, name the shape-of-solution.** If the user is "on the fence" or the ask is ambiguous, don't produce a full spec — produce the shape-of-solution question(s) first and get an answer. See *Scope Disambiguation (v4.48.0)*.

### Testing Philosophy
`tests/uat.js` preference order (baked into header comment post-v4.42.3-audit):
1. **Behavioral smoke** — extract function bodies via `Function()` or `vm.runInNewContext`, feed fixtures, assert output
2. **Structural regex** — assert a specific pattern exists or doesn't
3. **Regression guards** — after deleting code, assert the deleted thing stays deleted
4. **Dynamic consistency** — cross-check across surfaces (e.g. all `MILESTONE_CHECKS` IDs appear in `MILESTONE_DEFS`)
5. **AVOID** pure function-name-presence (`js.includes('function foo(')`) — proves nothing beyond what downstream assertions already do
6. **AVOID** hardcoding versions in 3+ places

**Search before adding tests.** Suite is at 3,887 assertions; duplicates slip in easily.

### Regression-Guard Tombstones
After deleting dead code, add a UAT assertion that fails if it reappears. Keeps 3–4 versions, then retires. Currently guarded:
- `function _renderAnaTopics` — must not reappear (v4.42.2)
- `pctSum += pct; count++` readiness aggregation — must not come back (v4.42.4)
- `#weak-banner` HTML — must not reappear (v4.41.0)
- `renderTodaysFocus()` must NOT be called from `finish()`/`submitExam()` (FLIP guard, v4.42.0)
- Bare `max_tokens: <literal>` — must not reappear (v4.42.5 magic-number extraction)
- `openGuidedLab` whitelist array — must not reappear (v4.42.5 whitelist trap fix)
- Scattered `let _tbTraceState`, `let _tbInspPrev*`, `let _tbStpState`, `let _tbStpPrevRoles`, `let _tbInspectorKeydownBound`, `let _tbTracePanelDragBound`, `let _tbInspectorPopDragBound`, `let _tbConfigPopDragBound` — consolidated into `_tbUiState` (v4.62.4 Thursday tech-debt sweep). New TB UI transients should be added as nested fields on `_tbUiState`, not new top-level `let` declarations.
- Per-overlay `tbRenderCanvas` re-wraps (trace + STP used to each `const orig = tbRenderCanvas; tbRenderCanvas = function() {...}`) — collapsed into the single `_tbOverlayRegistry` + `tbRegisterOverlay(fn)` pattern (v4.62.4 Thursday tech-debt sweep). New canvas overlays should call `tbRegisterOverlay(overlayFn)` once; they automatically run after every `tbRenderCanvas()` call. Do not re-introduce self-wrapping.
- 8 dead functions removed v4.62.4 (grep-audited to zero callers across app.js + index.html + tests/ + sw.js): `classOfIp`, `wildcardToMask`, `setPortMode`, `nextPortQ`, `nextPortFamilyQ`, `pickPort`, `renderPortFocusInfo`, `tbOpenArpDialog`. If you find yourself "reintroducing" any of these by name, check first whether the real need is a legitimate new function or a resurrection — legacy stubs without callers tend to accumulate indefinitely.
- `tbProcessCliCommand` 452-line if/else chain (v4.62.4 Thursday tech-debt sweep, issue #126) — refactored to a declarative `_TB_CLI_COMMANDS` dispatch table + ~37 isolated handler functions (`_cliShowArp`, `_cliPing`, `_cliIpconfig`, `_cliHelp`, etc.). Each handler is 3-20 lines and receives `(dev, cmd)`. New function body is 8 lines — iterate the table, call `_tbCliMatches(entry.match, cmd)`, return `entry.handler(dev, cmd)`. Match spec can be `string | string[] | (cmd)=>bool`; arrow matchers MUST use `cmd` as the parameter name (not `c`) because UAT structural regex checks like `/cmd\.startsWith\('ping '\)/` and `/cmd === 'ipconfig'/` depend on the literal parameter text. Critical ordering: `dig +dnssec` entry must precede generic `dig`/`nslookup` entry (more-specific-first wins). When adding a new CLI command: write an `_cli<Name>(dev, cmd)` handler, append one line to `_TB_CLI_COMMANDS` — do NOT reintroduce if/else chains in `tbProcessCliCommand`.
- TB 3D View dynamic-import contract (v4.63.0, issue #199 Phase 1) — `tb3d.js` + `vendor/three/*` MUST load lazily on first 🧭 3D View click, never at app init. Violating this ships 1.3 MB of Three.js to every user regardless of whether they ever open 3D. UAT guards three angles: `index.html` must not contain `<script src="tb3d.js">`, `app.js` must not contain a top-level `import './tb3d.js'`, and `sw.js` SHELL_ASSETS must not precache `/vendor/*`. Playwright verifies behaviorally that zero `/vendor/three/*` fetches fire on initial page load; only the 3D View pill click triggers them. When adding new 3D-related code, prefer extending `tb3d.js` over creating new sibling modules so the single dynamic-import boundary remains the contract surface. If a new heavy dependency is needed, add it as a separate lazy-loaded module with its own regression guard — never import from `app.js` top-level.
- TB 3D View render-only contract (v4.64.0, issue #199 Phase 2) — `tb3d.js` MUST NOT mutate `_tbUiState` or any other app.js state directly. It receives trace state through the exported `setTraceState(traceState)` function only, called from app.js inside `tbRenderTraceCanvasState()`. Playback buttons in the 3D chrome (`tb3dTracePlay/Pause/Step/End/Speed`) are thin wrappers in app.js that delegate to the v4.61.0 globals (`tbTracePlay`, `tbTracePause`, `tbTraceStep`, `tbEndTrace`); they MUST NOT move state ownership into tb3d.js. UAT guards: `tb3d.js` source must not contain `_tbUiState.X =` assignments; `tbRenderTraceCanvasState` must still exist (2D trace path stays independent); `#tb-trace-panel` must not be removed from index.html. Rationale: state duplication between 2D and 3D would desync on every edge case (reduced-motion, Back-to-2D mid-trace, speed changes) and triple the surface area. The render-only discipline is what keeps the 2D + 3D views in lockstep for free.

### Progressive Disclosure
- Marathon Mode (30 / 45 / 60-Q bulk presets) hidden until `loadHistory().length > 0`
- 4 drills collapsed into a single `#page-drills` launcher (+1 click, cleaner first-visit)
- Settings `<details>` houses Clear Wrong Answers + Export/Import (off-path for casual use)

`is-hidden` is the standard CSS class for conditional visibility.

### `saas-gated` Label
Items tagged `saas-gated` on either board are **frozen** until paid-user SaaS pivot triggers. Do NOT pull without explicit pivot direction. Currently gated:
- [#21](https://github.com/oremosu98/networkplus-quiz/issues/21) — Wrap 78 global variables into state objects
- [#55](https://github.com/oremosu98/networkplus-quiz/issues/55) — Split `styles.css` monolith
- [#123](https://github.com/oremosu98/networkplus-quiz/issues/123) — Social proof counter (needs real users)
- [#135](https://github.com/oremosu98/networkplus-quiz/issues/135) — Per-user API cost telemetry (pointless at n=1)
- [#136](https://github.com/oremosu98/networkplus-quiz/issues/136) — Entitlements + pricing-tier quotas
- [#137](https://github.com/oremosu98/networkplus-quiz/issues/137) — Cost-floor model + usage distribution
- [#138](https://github.com/oremosu98/networkplus-quiz/issues/138) — Module-split `app.js` (v5.0 trigger)
- [#139](https://github.com/oremosu98/networkplus-quiz/issues/139) — Readiness-algorithm tuning

### Magic-Number Constants (v4.42.5)
11 named constants at `app.js:22–43`:
- `EXAM_PASS_SCORE = 720`, `EXAM_QUESTION_COUNT = 90`, `EXAM_MAX_SCORE = 900`
- `DOUBLE_CLICK_MS = 400` (topology canvas)
- `VXLAN_VNI_MAX = 16777215` (RFC 7348)
- `MAX_TOKENS_{GENERATION, VALIDATION, TEACHER_DEFAULT, TEACHER_LONG, TEACHER_COACH, TEACHER_BRIEF}`

**UAT regression guard** `!/max_tokens:\s*\d+/.test(js)` fails loud if bare literals reappear in any AI call site.

### Tier-Threshold Anchoring (v4.45.1 lesson)
When picking thresholds for user-visible tier labels (Domain Mastery badges, tier multipliers, "you're ready" signals), **anchor to domain reality, not even quartiles**. The v4.45.0 Domain Mastery card originally used 60 / 75 / 85 (clean 15-pt bands above Novice) because it felt "mathematically clean" — but that put users at 70% (who would likely pass the real CompTIA exam) into "Developing," which is pedagogically wrong. Real N10-009 raw-accuracy pass is ~70–75% per form. Corrected to 55 / 70 / 85 in v4.45.1.

**Principle**: a tier labeled "Proficient" must mean something operationally true ("you'd pass the real exam today"). If your threshold contradicts domain reality, users will catch it and the label erodes trust.

### Analytics: Prescriptive over Descriptive (v4.45.0 / v4.45.2 lesson)
Analytics cards that just *show data* without telling you what to do are low-value. Two cards were deleted in v4.45.0–v4.45.2 for being descriptive-only:
- **Difficulty × Topic Heatmap** — sparse matrix, no action tied to cells
- **Question Type Breakdown** — 3 of 5 rows had sample sizes too small to act on
- **Subtopic Weak Spots** — keyword frequency duplicated the homepage `#todays-focus` chip row with less actionable output
- **Practice Drills stats card** — each drill has its own in-drill dashboard

Replacements (v4.45.0) follow the **prescriptive** principle — every card tells you what to do next:
- **Domain Mastery** — 5 rows with drill buttons that fire `focusTopic()` on the domain's weakest topic
- **Wrong-Answer Patterns** — clusters by CAUSE (negation / domain / PBQ-type / Hard-difficulty) with coaching copy + drill buttons

Criterion for any future Analytics card: *"What does the user do with this?"* If there's no clear action, it belongs in the Progress page (reflective) or doesn't belong at all.

### Learn-Tab Lesson Authoring (v4.43.6 / v4.43.7)
Subnet Trainer lessons use a **stepped pedagogical format** — one step per theory card, visible example ranges with a ✅ on the correct answer, worked example + "bigger example" to force generalization, cheat-sheet table, and a pro-tip card. See Lesson 4 (Block Size Method) and Lesson 5 (Network & Broadcast) as reference. When adding new lessons, follow this structure — one theory card per logical step rather than dense paragraphs.

### Feedback Goes Where The Eye Is Looking (v4.47.2 lesson)
When confirming a state change, put the feedback **in the user's line of sight**, not above or below it. v4.47.0/4.47.1 tried toast (bottom) + requirements panel (above toolbar, above the fold when user scrolled down to the canvas) + scroll-into-view with `block: 'nearest'` — all technically happened, all invisible to a user looking at the canvas. v4.47.2 fixed it by replacing the canvas empty-state content with a scenario card IN the canvas — where the user's eye lands after a modal closes. **Principle:** on big tall canvases / long pages, "rendered somewhere" is not the same as "noticed." Before shipping UI feedback, ask: *if the user is looking at location X when they click, what do they see change at location X?*

### Scope Disambiguation — Surface the Right Question Up Front (v4.48.0 lesson)
When planning a feature, enumerate the decisions that could radically change the shape of the ship. In the scenario-expansion work (v4.47.0 → v4.48.0), I surfaced "light vs strict grading" but never "static requirements vs auto-populate canvas" — which was the user's actual mental model all along. Cost: three release iterations (v4.47.0 text requirements → v4.47.1 picker → v4.47.2 in-canvas card → v4.48.0 auto-build) before we landed at the thing that was asked for on day one. **Fix**: when drafting the ship plan, list *shape-of-solution* decisions separately from *polish* decisions. Ask the user "how do you want this to feel?" not just "which rules do you want?". Examples of shape-of-solution questions to ask up front:
- Static data vs. generated?
- Is this additive to existing content, or replacing it?
- Does this live on the canvas or on the toolbar or in a modal?
- Do they expect the feature to be destructive (replace state) or non-destructive (layer on)?

### Live-Verify UX Changes via Chrome MCP Before Declaring Done (v4.47.2 / v4.48.0 discipline)
Big UX changes (canvas rendering, multi-step flows, modal → canvas interactions) should be tested live in Chrome before being called done. UAT catches structural regressions but not "does this actually feel right when I pick a scenario." The Claude-in-Chrome MCP (`mcp__Claude_in_Chrome__*`) runs fine against local `python3 -m http.server 3131` and prod — use it to navigate + click + screenshot. Twice this session UAT was green but the real UX was broken: (1) v4.47.2 picker-flow "worked" per UAT but feedback was invisible to a user looking at the canvas; (2) v4.48.0 scenarios built devices but cables silently failed for complex topologies because `_tbMkCable` didn't auto-provision interfaces — caught only by running all 15 scenarios live and seeing "cables: 0" in the diagnostics. **Routine**: any release that touches render paths, empty-states, or multi-step flows → Chrome-verify the happy path before commit. UAT alone isn't enough.

### 🛑 Testing Discipline — NEVER write to user-state localStorage on prod (v4.81.0–v4.81.3 incident)

**Hard rule, no exceptions.** When using the Chrome MCP `javascript_tool` (or any tool that runs JS in the user's connected browser), **NEVER call `localStorage.setItem`, `localStorage.removeItem`, or `localStorage.clear` on the production URL** (`https://networkplus-quiz-sable.vercel.app` or any `*.vercel.app` host). The user is logged in to that browser; their real progress, streak, history, API key, and drill mastery live in `nplus_*` localStorage keys. Direct writes from console / MCP overwrite real data with no undo unless the v4.81.2 auto-backup happened to capture the previous state.

**This rule was bought with real damage.** During v4.81.0 → v4.81.1 NBM-flicker debugging, "fake state" was injected via `localStorage.setItem('nplus_history', JSON.stringify(fakeData))` on the user's connected prod browser to try to reproduce the bug. That overwrote the user's real quiz history, streak, exam date, daily goal, and API key. With no backup at the time, the loss was permanent. The user lost ~51 days of quiz session log (drill mastery, wrong bank, SR queue, milestones survived because they live in separate keys I didn't touch — which is the only reason this didn't end worse).

**Three approved alternatives — pick one before any state-mutating investigation:**

1. **Local server** — `python3 -m http.server 3131` from the project root, then navigate Chrome MCP to `http://localhost:3131`. The local HTML/JS is the same code; localStorage writes there don't touch the user's prod browser. Best default.
2. **Vercel preview deploy** — push the work-in-progress branch; Vercel auto-builds a preview at `https://networkplus-quiz-sable-git-<branch>-oremosu98.vercel.app`. Different origin = different localStorage namespace, so writes there are safe even from the same browser.
3. **Incognito Chrome window** — open a fresh incognito profile that has never been logged in to the app. Its localStorage starts empty. Inject whatever fake state you need. Closes when you close the window.

**What you CAN do on connected prod (read-only patterns):**

- DOM inspection via `read_page`, `find`, `get_page_text`
- Screenshots via `screenshot`
- `mcp__Claude_in_Chrome__javascript_tool` for **read-only** evaluations: `localStorage.getItem(...)`, `Object.keys(localStorage)`, computed-style lookups, MutationObserver instrumentation that only LOGS (doesn't mutate)
- `mcp__Claude_in_Chrome__javascript_tool` writes to `window.__debug = ...` or other non-app namespaces are fine; just not the `nplus_*` namespace.

**Recovery layers** (already shipped, all v4.81.x):

- v4.81.2 `_takeAutoBackup()` runs daily on `DOMContentLoaded`; rolling 7-day window; restore via Settings → Automatic backups
- v4.81.3 `_emitProdConsoleBanner()` fires a giant red console warning on every prod page load, telling future agents/devs not to write to localStorage from the console
- v4.81.3 `.env-badge` pill renders in the top-right corner: red "PROD" on prod, green "DEV" on local — visual environment confirmation
- v4.81.3 `_maybeExportReminder()` surfaces a toast every 14 days suggesting Settings → Export Data so a copy lives outside the browser
- UAT regression guard: any literal-string `localStorage.setItem('nplus_…', ...)` outside the STORAGE namespace fails UAT — forces all state writes through the typed STORAGE table where they're discoverable + auditable

**Pre-commit hook check** (`.githooks/pre-commit`) scans staged files for the pattern `mcp__Claude_in_Chrome__javascript_tool` paired with `localStorage.setItem` and warns loudly. Belt-and-suspenders — false positives possible, but the warning prompts a sanity check.

If you ever find yourself thinking "I just need to inject one quick test value to repro the bug" on prod — STOP, switch to the local server or preview deploy. The 30 seconds it takes to spin up `python3 -m http.server` is infinitely cheaper than corrupting a user's real state.

### Scenario `autoBuild(state)` + Auto-provisioning Helpers (v4.48.0)
Scenarios seed the canvas via a two-helper pattern that hides the 13-field device-object boilerplate:
- `_tbMkDev({ type, x, y, hostname, ip?, gateway?, iface? })` — one interface by default.
- `_tbMkCable(a, b, type='cat6', aIdx=0, bIdx=0)` — **auto-provisions additional interfaces** on either endpoint if the requested index doesn't exist yet. A hub device (MPLS-Core, TGW, firewall) can be connected via `_tbMkCable(hub, x, 'fiber', 0)`, `_tbMkCable(hub, y, 'fiber', 1)`, `_tbMkCable(hub, z, 'fiber', 2)` without pre-declaring 3 interfaces on `hub`.

**Without the auto-provision, cables silently throw** inside the `tbLoadScenarioWithBuild` try/catch, leaving scenarios with 0 cables while the device list looks fine — a very misleading failure mode. When authoring new scenarios, iterate in a Chrome session and verify `tbState.cables.length === expected` after each scenario select.

## Deployment

### Two paths — prefer push
1. **Canonical (preferred)** — `git push origin main` → GitHub Actions runs UAT + Playwright + `tech-debt.js` → Vercel auto-deploys → `deploy-verification.yml` smoke-tests prod. **Branch protection requires the "UAT + Playwright" status check** before merge; admin bypass only for emergencies.
2. **Local quick-ship (bypasses CI gates)**:
   ```bash
   export PATH="$HOME/.nvm/versions/node/v20.20.2/bin:$PATH"
   cd "/Users/simioremosu/Desktop/Dev Projects/networkplus-quiz"
   npx vercel --prod --yes
   ```
   **Do NOT use without committing first (or immediately after).** Git↔prod drift cost us 6 versions (v4.39→v4.43.1) before being caught in catchup commit `415cab6` on 2026-04-16. If circumstances force a local deploy, schedule a catchup commit before session ends.

Production URL: https://networkplus-quiz-sable.vercel.app

### Version bumps
Use `node scripts/bump-version.js <new> "<description>"` — updates `APP_VERSION` in `app.js` + `CACHE_NAME` in `sw.js` + version badge in `index.html` + `package.json` + **prepends a stub row at the top of the CLAUDE.md version history table** in one shot. **Do NOT hand-edit partial** — UAT has consistency assertions across all 4 code surfaces.

**Release flow discipline (learned the hard way):**
1. Run `bump-version.js` first — it writes CLAUDE.md.
2. **Read CLAUDE.md again before expanding the stub row** — the bump script mutated it, any prior Read snapshot is stale and the next Edit will hit a file-state race.
3. Expand the stub into a full detailed row in-place (it's already at the top — no move needed).
4. Run UAT → commit → push. The CLAUDE.md history stays consolidated inside the same commit as the code change, not in a trailing docs-only follow-up.

Pre-v4.43.9 the script appended to the bottom of the table and every release required a "move row to top" Edit that repeatedly hit the race above. Fixed to prepend correctly.

### Preview deployments
Every feature branch auto-builds a preview at `https://networkplus-quiz-sable-git-<branch>-oremosu98.vercel.app`. Use for risky changes (SW rewrites, API contract changes, large refactors). Squash-merge to main auto-deletes the branch.

## Branching Strategy
**Trunk-based** — direct commits to `main`, no `stage`/`preprod` branch. Local UAT (`node tests/uat.js`) gates every push.

**Add a `stage` branch only when one of these fires:**
1. Paying users on the cert SaaS pivot (a broken deploy starts costing real money)
2. A backend with DB migrations (need a non-prod target to run them against)
3. A second person joins (contractor, partner, beta team needs a non-prod URL)

Until then, stage is pure overhead — Vercel preview deploys give 90% of its value at 0% of the friction.

## Local Development
```bash
cd "/Users/simioremosu/Desktop/Dev Projects/networkplus-quiz"
python3 -m http.server 3131
# Open http://localhost:3131
```

### Pre-commit hook (v4.62.4 / issue #194)
`.githooks/pre-commit` runs `node tests/uat.js` locally before every commit and blocks the commit on failure. Skips when only docs-type files (CLAUDE.md, CHANGELOG.md, memory files) are staged so trivial docs touches stay fast. Also runs the pre-existing CLAUDE.md-freshness check. Hook is wired via the `prepare` script in `package.json` setting `core.hooksPath` to `.githooks`, so `npm install` activates it automatically on clone.

- **UAT failure** → commit blocked; hook tails the last 25 lines of UAT output so you see what broke without re-running.
- **node not in PATH** → UAT skipped with a warning; CI still enforces on push.
- **Bypass** (docs-only / emergency): `git commit --no-verify`.

## Test Suite
```bash
export PATH="$HOME/.nvm/versions/node/v20.20.2/bin:$PATH"
cd "/Users/simioremosu/Desktop/Dev Projects/networkplus-quiz"

node tests/uat.js                # UAT (3,887 assertions; embeds validation-audit gate)
node tests/tech-debt.js          # CI thresholds — long-function count, LOC, globals
node tests/validation-audit.js   # 20-Q broken-corpus fixture, ≥60% catch / 0 FP
node tests/deploy-verify.js      # Post-deploy smoke against prod
npx playwright test              # E2E (tests/e2e/app.spec.js)
```

`tests/tech-debt.js` runs in CI on every push — breaches auto-file issues on [Board #1](https://github.com/users/oremosu98/projects/1) with `tech-debt` + `priority: medium` labels. Scheduled weekly sweep on Thursdays.

## Version History

> **Older releases** (v3.0 through v4.72.0 — 143 rows) live in [CHANGELOG.md](./CHANGELOG.md). The last 11 releases stay inline here for quick reference. Reorg established during the v4.42.3 maintenance pass; most recent trim was during v4.80.0 (Codex round-4 polish — ACL Builder default scenario shipped, Custom Quiz hide-by-default reverted with lesson captured).

| Version | Features Added |
|---|---|
| v4.81.28 | **🐛 SR review — 3 follow-up bugs after v4.81.27 dogfood (user said "still buggy")**. v4.81.27 fixed the render path so multi-select cards finally showed content (read-only options + self-grade banner + 3 confidence buttons), but a comprehensive audit tonight found 3 more bugs — one critical that broke the self-grade path entirely, and two latent issues from the original SR queue design. **Bug 1 (CRITICAL — blocks the user)**: `srMarkConfidence` had a defensive `!_srSession.revealed` early-return guard. For self-grade cards, `revealed` stays `false` (the user never picks an option that triggers `srPickAnswer` to set it true), so clicking any of the 3 confidence buttons returned early and **did absolutely nothing**. User stuck on every multi-select card after v4.81.27 deploy. **Fix 1**: set `_srSession.revealed = true` in the self-grade render branch. Self-grade is "already revealed" because the explanation renders immediately, so flipping the flag here is semantically correct AND it preserves the existing defensive guard for the auto-grade path (revealed stays false until the user picks an option in MCQ mode). **Bug 2 (latent since SR queue shipped, surfaced by v4.81.27 fix)**: `addToSrQueue` re-enrollment fired `_srSchedule(entry, 'wrong')` but never refreshed the question payload. Any legacy entry with the v4.81.27-fixed `answer: null` corruption would stay corrupt forever — even after re-encounter — so the v4.81.27 self-heal couldn't actually heal anything. **Fix 2**: on re-enrollment, overwrite `entry.options / answer / answers / items / correctOrder / type / topic / difficulty / explanation` with the fresh values from the current `q`. Legacy corrupt entries now self-heal on the next time the user gets that question wrong. **Bug 3 (silent corruption since SR queue shipped)**: `addToSrQueue` accepted any `q.type`. Order / cli-sim / topology PBQs were enrolled into the queue but rendered with empty bodies (no `card.options`, no items rendering, just stem + self-grade fallback with no useful content). **Fix 3**: filter at enrollment — `allowedTypes = new Set(['mcq', 'multi-select'])`. Order / cli-sim / topology types return `null` without enrolling. Wrong-bank still tracks the miss; SR queue stays focused on review-able question types. **UAT**: **6 new v4.81.28 assertions** — 3 structural (revealed=true in self-grade branch, payload refresh on re-enroll, allowedTypes filter), **3 vm-fixture behavioral tests** — (a) end-to-end self-grade flow: render multi-select card → assert `revealed=true` is set → call `srMarkConfidence('correct-confident')` → assert index advanced + tally updated (the exact post-v4.81.27 bug case); (b) re-enrollment heals legacy null-answer entry: pre-seed queue with `answer: null` corrupt entry → re-enroll with valid `answer: 'C'` → assert payload now correct; (c) order/cli-sim/topology types rejected at enrollment: 3 PBQ types return null + 0 enroll, mcq + multi-select both enroll + 2 in queue. Total UAT: 4659 → **4665** passing. Validation audit still 64.7% catch / 0% FP. Playwright: **79/79** still green. Cache: `netplus-v4.81.28`. ~120 LOC across `app.js` (3 fixes) + `tests/uat.js` (6 assertions). **108th ship**. **Sunday's 23rd ship** — fifth dogfood-driven SR/schema fix tonight in the cluster .24 → .25 → .26 → .27 → .28. The pattern across the cluster: tight feedback loops (user spots → audit → fix → ship in 30-60 minutes) catch bug families that scanners + structural UAT alone miss. The mantra crystallized: **every new render path that consumes localStorage state needs a vm-fixture in its UAT, before it ships.** |
| v4.81.27 | **🐛 SR review render — three latent bugs uncovered by user dogfood (multi-select stem with no options visible)**. User screenshot of SR Review page: a multi-select question stem rendered correctly ("(Choose TWO) A network architect is evaluating WAN technologies...") but **no answer options below it**. Empty card body. Investigation revealed three layered bugs that compounded: **(1) Schema mismatch in `_renderSrCard`** — the renderer expected `card.options` as an array (`Array.isArray(opts)` check) and built buttons via `opts.map((o, idx) => ...)`, but the live app stores options as a letter-keyed object `{A: '...', B: '...', ...}` matching `renderMCQ`. The `Array.isArray()` check failed, options never rendered. Same bug class as v4.81.5 diagnostic-options schema mismatch. **(2) Answer-preservation bug in `addToSrQueue`** (latent since SR queue shipped) — `answer: typeof q.answer === 'number' ? q.answer : null` had the wrong type guard. The app stores MCQ answers as letter strings (`'A'`, `'B'`), so `typeof 'A' !== 'number'` always evaluated false → every MCQ ever enrolled was saved with `answer: null`. Auto-grading was impossible even if options had rendered. **(3) Multi-select unsupported entirely** — even with the above fixed, a multi-select card has 5 options + an `answers: ['A','C']` array; the SR review card was built as a simple radio-style flashcard. **Three-fix ship**: **Fix A**: rewrote `_renderSrCard` to normalise options into `optionLetters[]` + `optionMap{}` regardless of stored shape (object or array), then build buttons by iterating sorted letters. Coerces numeric-index legacy answers to letter form for the auto-grade compare. **Fix B**: changed `addToSrQueue` to preserve `q.answer` as-is (`(q.answer != null) ? q.answer : null`) — strings, numbers, both work. **Fix C**: added a self-grade fallback path. When the card can't be auto-graded (`type === 'multi-select'` OR `answer === null` from the legacy enrollment bug), show options READ-ONLY (so the user can reason), display the explanation immediately, and present three confidence buttons (Confident / Uncertain / Wrong) without an auto-check. Same SM-2 outcomes route through the existing `srMarkConfidence`. Pedagogically valid because the user still re-encounters the question and self-rates retention; just no automated grading. Multi-select-aware review with checkboxes can land later (v4.81.28+). **Renamed state field**: `_srSession.pickedIdx` → `pickedLetter` to match the new letter-based comparison; legacy `pickedIdx: null` retained for any external observer that might read it. **CSS**: new `.sr-options-readonly` (dashed-border read-only options) + `.sr-self-grade-banner` (amber italic banner explaining the fallback). **Audit-pattern lesson reinforced (3rd time tonight after v4.81.5/.16/.26)**: behavioral vm-fixtures are mandatory for any new render path that consumes localStorage state. The original v4.81.0 SR review ship had no vm-fixture covering the render — would have caught all three bugs immediately. **UAT**: **10 new v4.81.27 assertions** — 7 structural (addToSrQueue answer preservation + tombstone for old idx-based compare + Object.keys().sort() pattern + srPickAnswer letter signature + canAutoGrade branch + .sr-self-grade-banner CSS + .sr-options-readonly CSS), **3 vm-fixture behavioral tests** — (a) MCQ with letter-keyed options renders 4 buttons with `data-letter="A"` through `data-letter="D"`, no fallback banner; (b) multi-select card falls back to self-grade banner + read-only options + 3 confidence buttons; (c) legacy MCQ with corrupt null answer (the addToSrQueue pre-fix bug) also falls back to self-grade — proves the existing-queue corruption is handled gracefully without losing those cards from the user's review queue. Total UAT: 4649 → **4659** passing. Validation audit still 64.7% catch / 0% FP. Playwright: **79/79** still green. Cache: `netplus-v4.81.27`. ~180 LOC across `app.js` (renderer rewrite + addToSrQueue fix + state rename) + `styles.css` (fallback CSS) + `tests/uat.js` (10 assertions). **107th ship**. **Sunday's 22nd ship** — fourth dogfood-driven schema-mismatch fix tonight (.24 verifier sync, .25 order-question, .26 daily-goal, .27 SR review). The pattern is overwhelmingly clear: every new render path needs a vm-fixture in its UAT, before it ships. |
| v4.81.26 | **🐛 Settings Health card daily-goal schema mismatch (v4.81.11 carry-over, user dogfood)**. User screenshot of Settings → Study Setup showing "DAILY GOAL: Not set — pick something you can hit every day" in the Health card AND the Daily Goal input below showing "100" with a Save button. Two surfaces telling opposite stories on the same screen. **Root cause** — schema mismatch shipped in v4.81.11: `renderSettingsHealthCard()` was reading `STORAGE.DAILY_GOAL` as a JSON object `{goal: N, date: "YYYY-MM-DD", current: N}`, but `setDailyGoal()` writes a plain string number (e.g. `"100"`). `JSON.parse("100")` returns `100`, then `(100 && typeof 100.goal === 'number')` is false, so the Health card always reported "Not set" even when the user had explicitly saved a goal. Same bug class as v4.81.5's diagnostic options-render schema mismatch — direct hit on `feedback_behavioral_fixtures.md`. The original v4.81.11 vm-fixture only exercised the API-key path, never tested the daily-goal path; that gap is exactly what the discipline note warned about. **Two affected sections** (both fixed): (1) Section 3 "Daily goal" — now reads via `parseInt(localStorage.getItem(STORAGE.DAILY_GOAL))` matching `setDailyGoal`'s write format, with a `isUserSetGoal` flag that distinguishes user-set values from the `DEFAULT_DAILY_GOAL` fallback (the row should say "Not set" when only the default is in effect, not when 20 is the explicit user choice). (2) Section 5 "Today's progress" — was reading `dg.current` from the same fictional JSON object; now uses `getTodayQuestionCount()` (sum of `e.total` across history rows with today's date), matching what `renderDailyGoalCard()` does on the homepage so the two surfaces stay in lockstep. **UAT**: **3 new v4.81.26 assertions** — **vm-fixture** that simulates 3 storage states: (a) no goal set → Health card reads "Not set" + Today reads "Set a daily goal first" (correct), (b) plain string number "100" written via `setDailyGoal`-style → Health card reads "100 questions / day" + Today reads "0 / 100 questions" (the bug case — was always failing pre-fix), (c) legacy/junk JSON write → still gracefully handled as "Not set" (parseInt yields NaN). Plus 1 tombstone asserting the buggy `dg.goal` schema check is removed, plus 1 wiring assertion confirming `getTodayQuestionCount` is the source of truth now. The vm fixture is the kind of test that should have shipped with v4.81.11 — adding it now closes the gap. Plus the existing v4.81.11 vm fixture got new ctx stubs (`getDailyGoal`, `getTodayQuestionCount`, `parseInt`, `Number`) so it stays passing across the refactor. Total UAT: 4646 → **4649** passing. Validation audit still 64.7% catch / 0% FP. Playwright: **79/79** still green. Cache: `netplus-v4.81.26`. ~70 LOC across `app.js` + `tests/uat.js`. **106th ship**. **Sunday's 21st ship** — third dogfood-driven fix tonight (v4.81.24 verifier sync, v4.81.25 order-question, v4.81.26 schema mismatch). Pattern reinforced: behavioral vm-fixtures are mandatory for any new render path that consumes localStorage state. The original v4.81.11 ship had a structural-only "Health card surfaces 5 rows" UAT — that level of testing missed the schema bug entirely. |
| v4.81.25 | **🐛 Order-question quality fix — Implement-before-Verify invariant + conflated-step rejection (user dogfood, exam-related)**. User screenshot of a Hard ordering question on the CompTIA troubleshooting methodology: the "correct order" the app showed had item 4 = *"Document findings AND implement a permanent solution or workaround"* — and the order placed verify (item 3) BEFORE this conflated implement+document item. **Two structural bugs in one question**: (1) item 4 conflated CompTIA Step 5 (Implement) with Step 7 (Document), which are non-adjacent in the methodology — Step 6 (Verify) sits between them; (2) even given the malformed item, the order put verify before implement, which is logically impossible (you can't verify a solution you haven't implemented yet). **Why the existing validator missed it**: `_tbTroubleshootingOrderOk` (v4.62.2) had a tight stem-detection gate requiring "comptia" + "methodology" / "X-step" — but the user's stem just said *"Arrange the following troubleshooting steps in the correct order when diagnosing a network connectivity issue."* No "comptia". No "methodology". So the existing methodology rules didn't fire. **Three-layer fix**: **Layer 0 (gate widening)**: validator now also fires on (a) generic "troubleshooting steps" + ordering keywords ("order"/"sequence"/"arrange") in the stem, (b) "diagnostic" + ordering, (c) item-keyword combo (Identify-the-problem + Document-findings both present) regardless of stem wording — that last one is the most robust because if the items themselves describe the methodology, we should enforce its rules even if Haiku's stem wording is loose. **Layer A (Implement-before-Verify ordering invariant)**: regex matches `\bimplement(?:ing)?\b ... \b(?:solution|fix|workaround|change|plan)\b` for the implement item, `\b(?:verify|verification)\b ... (?:solution|connectivity|functionality|resolved|restored|preventive|fixed)\b` for verify. If both present in different items and `posImpl >= posVer`, reject. **Layer B (conflated-step rejection)**: regex `\bimplement(?:ing)?\b ... \b(?:solution|fix|workaround|plan)\b` AND `\bdocument(?:ing)?\b ... \b(?:findings?|actions?|outcomes?)\b` in the same item — auto-reject. Steps 5 and 7 are non-adjacent; combining them in one item is structurally malformed regardless of which order the grader picks. **The bug class**: same family as v4.81.16 (multi-select stem-vs-answer-count + canonical fact lock) — generic structural validator + specific content invariant. Order-question equivalent. **UAT**: **9 new v4.81.25 assertions** — 5 structural (gate-widened keywords, Identify+Document combo gate, implementIdx + verifyIdx variables declared, Implement-before-Verify ordering enforcement, conflated-keyword detection), **4 vm-fixture behavioral tests** — (a) the EXACT user-reported bug case rejected (4 items including "Document findings AND implement..." with the same correctOrder the app produced); (b) Implement-after-Verify rejected even with clean (non-conflated) items; (c) correct 6-step methodology ordering accepted (regression guard, no false positive); (d) non-troubleshooting order question (OSI layer ordering) passes through unchanged. Total UAT: 4637 → **4646** passing. Validation audit: still 64.7% catch / 0% FP — new layers add rejection paths without false-positive any of the 23 broken-corpus or curated-good fixtures. Playwright: **79/79** still green. Cache: `netplus-v4.81.25`. ~80 LOC across `app.js` + `tests/uat.js`. **105th ship**. **Sunday's 20th ship** — second exam-related dogfood fix in the same evening (paired with v4.81.24 fix-forward). Pattern repeats: the user's exam prep is the highest-fidelity quality engine we have. |
| v4.81.24 | **🔧 Deploy verification fix-forward — `REQUIRED_IDS` list missed in v4.81.23 cleanup**. v4.81.23 successfully removed the legacy `#todays-focus` element + 3 other compat-shim elements + their CSS + their render functions. CI/CD passed (UAT 4636/4636 + Playwright 79/79), site deployed, but the post-deploy `tests/deploy-verify.js` smoke test failed because its `REQUIRED_IDS` array hard-coded `'todays-focus'` as a critical DOM marker. Verifier ran on prod, found 16/17 elements present (`todays-focus` missing), opened issue #267, marked Deploy Verification as failed. Site itself was 100% functional — the verifier list just hadn't been updated to track the new canonical element name. **Fix**: replaced `'todays-focus'` with `'today-plan'` in `REQUIRED_IDS` (the consolidated card that replaces the 4 legacy stacked surfaces). One-line change in `tests/deploy-verify.js`. **Prevention**: added a UAT cross-check that parses `REQUIRED_IDS` out of `tests/deploy-verify.js` and asserts every ID in the list is present in `index.html`. Catches future drift locally before any push: if a future ship removes a DOM element listed in `REQUIRED_IDS` without updating the list, UAT fails immediately with a specific error message naming the missing IDs. **Audit-pattern lesson**: cleanup-pass ships that remove DOM elements need to also walk `tests/deploy-verify.js`'s `REQUIRED_IDS` (and any other hard-coded element-presence lists). Filed mentally as "external-test sync check" — same pattern as updating `tests/validation-audit.js` when a new validator helper joins the pipeline (v4.81.16 lesson). **Issue #267 closed** as part of this ship — auto-opened by Deploy Verification workflow on v4.81.23 failure. **UAT**: **1 new v4.81.24 assertion** — parses `REQUIRED_IDS` constant from deploy-verify.js, cross-checks every id against index.html, fails loud with the missing list if any drift. Total UAT: 4636 → **4637** passing. Playwright: **79/79** still green. Cache: `netplus-v4.81.24`. ~10 LOC. **104th ship**. **Sunday's 19th ship** — same-day fix-forward for v4.81.23's deploy-verification false alarm. The cleanup arc is fully closed now: v4.81.18 → v4.81.23 (consolidation + cleanup) + v4.81.24 (sync the verifier). |
| v4.81.23 | **🧹 Cleanup pass — remove legacy compat shims (DOM + functions + CSS) from v4.81.18-22 consolidation (user request: "consolidate all the necessary files")**. The v4.81.18→v4.81.22 consolidation series intentionally kept legacy DOM elements + render functions as hidden compat shims — partly for safety (UAT regression guards), partly to defer the cleanup risk. With the consolidated `#today-plan` card proven on prod across 5 ships, time to finish the job by removing all the dead weight. **Removed**: **(1) 4 hidden DOM elements** from `index.html` — `#todays-focus` (Weak Spots row), `#rotation-row` (stale chips), `#session-banner` (study plan), `#focus-banner` (v4.54.0 prescriptive surface). All were `is-hidden` and never rendered anything; just took up space in the DOM tree + cluttered devtools. **(2) 4 shim functions** from `app.js` — `renderTodaysFocus()`, `renderRotationChips()`, `renderSessionBanner()` (deleted entirely; callers now invoke `renderTodayPlan()` directly), plus `renderSetupFocusBanner()` reduced to a 1-line no-op stub for any external code that might still reference it. **(3) Orphan CSS** — `.todays-focus`, `.tf-label`, `.tf-chips`, `.tf-chip` (+ FLIP-rerank transition + reduced-motion gate); `.rotation-row`, `.rot-label`, `.rot-chips`, `.rot-chip`, `.rot-chip-meta` (+ reduced-motion gate); `.session-card`, `.session-card h3`, `.session-topic-row`, `.session-step-badge`, `.session-t-name`, `.session-t-reason`, `.session-est`; `.tf-bridges`, `.tf-bridge-btn`, `.tf-bridge-icon`, `.tf-bridge-label`, `.tf-bridge-arrow` (Subnet Trainer bridge moved to `.tplan-bridge-btn` in v4.81.21); `.focus-banner-v2`, `.focus-banner-v2 .fb-quote`, `.focus-banner-v2 .fb-body`, `.focus-banner-v2 .fb-text`, `.focus-banner-v2 .fb-meta`, `.focus-banner-v2 .fb-cta` + mobile breakpoint + reduced-motion gate; legacy v4.53.0 `.focus-banner` block (~80 lines) + `.focus-cta` + `@keyframes focusBannerFadeIn`; light-theme `.focus-banner` + `.focus-banner-v2` + `.focus-quote-mark` + `.focus-cta` + `.focus-text em` overrides. **(4) Updated callers**: `goSetup()` in app.js now calls `renderTodayPlan()` directly (replacing 3 separate shim calls); `_v453Init` (DOMContentLoaded handler) does the same; the DOMContentLoaded outer handler also consolidates. **Discipline kept**: kept the `body.hero-v2-active` CSS rule that hides `#daily-goal-card` (still functional in hero v2 mode). Kept `_v453Init` rendering of `renderTodayPlan` for setup-page first-paint. **UAT churn**: 30 tests had to be updated/retargeted — most were "element exists" or "function delegates to renderTodayPlan" guards that are now invalid. Converted to **tombstone** assertions ("element removed", "function removed", "CSS class removed") so the dead code can't sneak back via copy-paste. Net UAT: 4639 → **4636** (3 fewer assertions but coverage strengthened — tombstones lock the cleanup permanently). **Net code reduction**: **-338 LOC** across the 4 files (174 insertions, 512 deletions). Specifically: `index.html` -22 LOC (4 elements + comments), `app.js` -120 LOC (4 functions + reduced shim calls), `styles.css` -160 LOC (orphan classes), `tests/uat.js` -36 LOC (test churn). **Audit-pattern lesson**: the consolidation pattern is "keep compat shims first, remove on next pass." Worked well — gave 5 versions to validate the new consolidated card before deleting the safety net. **UAT**: **30 tombstone assertions** added (`function X removed`, `id="..." removed`, `.classname CSS removed`) so the dead code stays dead. Plus a few v4.53.0/v4.54.0 tests retargeted from "element exists" to "element removed" to track the retirement explicitly. Total UAT: 4639 → **4636** passing. Playwright: **79/79** still green. Cache: `netplus-v4.81.23`. **103rd ship**. **Sunday's 18th ship**. Closes the v4.81.18 consolidation arc cleanly — what was a 4-surface mess in v4.81.17 is now one canonical card with no legacy detritus left behind. |
| v4.81.22 | **🧹 Hide `#today-section` wrapper when all children effectively hidden (user dogfood after v4.81.21)**. User screenshot post-v4.81.21 deploy showed an empty pill-shaped card containing just the "TODAY" title — no daily goal ring, no streak defender, no daily challenge, no consolidated plan card, nothing. The wrapper was rendering as visible because `renderTodaySection()` always called `section.classList.remove('is-hidden')` regardless of children state. **Diagnosis** — historical assumption broke: the original v4.41.0 comment said *"Daily goal card is always visible, so the section is always shown"* — true at the time, but: (a) the v4.54.0 hero v2 redesign added `body.hero-v2-active #page-setup .today-section .daily-goal-card { display: none !important; }` which hides the goal card via CSS (the `is-hidden` class isn't applied, but the element doesn't render), (b) v4.81.18 consolidation made `#todays-focus`, `#rotation-row`, `#session-banner` permanent hidden compat shims, (c) v4.81.18's new `#today-plan` correctly hides itself when `isStudyPlanDoneToday()` returns true. With the user's state (plan done today, streak healthy, daily challenge done) — every candidate child was either hidden via class or hidden via the hero-v2 CSS rule. The wrapper had nothing to wrap, but stayed visible. Result: orphan "TODAY" pill with no content, exactly the same visual-noise pattern v4.81.18 set out to eliminate. **Fix**: rewrote `renderTodaySection()` to actually compute child visibility correctly: walks 4 candidate IDs (`#daily-goal-card`, `#streak-defender`, `#daily-challenge-card`, `#today-plan` — drops the v4.81.18 compat shims since they're permanently hidden anyway), checks `el.classList.contains('is-hidden')` for each, AND checks `body.hero-v2-active` to treat `#daily-goal-card` as effectively hidden when the hero v2 CSS rule applies. If no candidate is effectively visible, adds `is-hidden` to the wrapper itself. Else removes it (existing behavior). Early-bails-loop with `break` on first visible child for minimal cost. **Done shape** — when SR card + Pass Plan tile + Mode Ladder are doing all the prescriptive work and today-section has nothing meaningful to add, the page now flows cleanly from Pass Plan → SR review → Mode Ladder without the orphan TODAY pill. When ANY child has content (e.g. unfinished daily challenge, active streak warning, or active plan), the wrapper shows as before — no behavior change in that case. **Audit-pattern lesson reinforced**: container visibility must be computed from children's *effective* visibility, not just the `is-hidden` class. CSS hide rules (`display: none !important`) need to be considered too. Same lesson as v4.81.20 (audit miss extended beyond the named container) — this time extended to "audit miss in a single function's assumptions." **UAT**: **6 new v4.81.22 assertions** — 4 structure (#today-plan in candidate list, hero-v2-active accounted for, conditional add/remove of is-hidden, tombstone for the retired comment), **2 vm-fixture behavioral tests** — (a) section hidden when daily-goal is hero-v2-hidden + today-plan is is-hidden + others is-hidden (mirrors the user's exact dogfood state); (b) section visible when ANY candidate has content (daily-challenge-card visible despite hero-v2 hiding daily-goal-card). Total UAT: 4633 → **4639** passing. Playwright: **79/79** still green. Cache: `netplus-v4.81.22`. ~30 LOC across `app.js` (renderTodaySection rewrite) + `tests/uat.js` (6 new). **102nd ship**. **Sunday's 17th ship**. Closes the v4.81.18-cluster dogfood loop completely. The home page now shape-shifts cleanly between active-plan state, plan-done state, and post-session state without leaving orphan visual containers behind. |
| v4.81.21 | **🧮 Restore Subnet Trainer bridge in `#today-plan` card (v4.43.1 affordance)**. Closes the second of two follow-ups filed during the v4.81.18 consolidation (the first being v4.81.19 comma-split fix). The v4.43.1 bridge was a small "Drill in Subnet Trainer →" pill that rendered below the Weak Spots chips when 1+ subnet-heavy topics were in the plan (`Subnetting & IP Addressing` / `IPv6` / `NAT & IP Services`). It routed users to the deeper binary-breakdown UX in Subnet Trainer instead of the regular MCQ quiz drill. Was retired in v4.81.18 because the chip-row layout it was attached to got consolidated into the new card; the `WEAK_SPOT_DRILL_BRIDGES` constant + `openWeakSpotBridge` handler stayed (still used by the Subnet Trainer dashboard). **Restore**: extended `renderTodayPlan()` to walk the plan items, look up each topic in `WEAK_SPOT_DRILL_BRIDGES`, dedupe by `kind + (labId || '')` (mirror the v4.43.1 pattern — both Subnetting + IPv6 in plan should produce only ONE "Drill in Subnet Trainer" button, not two), render a `.tplan-bridges` row of `.tplan-bridge-btn` pill buttons after the chip strip. Each button onclick calls existing `openWeakSpotBridge(kind)` handler. **Visual treatment**: pill-shaped (border-radius 999px) with dashed white border that turns solid on hover, white-translucent background — visually subordinate to the primary chips + CTA but more affordant than plain text. Sits between the chip strip and the foot row so it's discoverable without competing for attention. **CSS**: `.tplan-bridges` (flex-wrap container) + `.tplan-bridge-btn` (pill button with icon + label + arrow). Mobile breakpoint resets the indent to 24px (matches other tplan-* margins). Reduced-motion gate neutralizes the hover lift + active scale. Defensive try/catch around the bridge-build logic so a missing `WEAK_SPOT_DRILL_BRIDGES` constant or unexpected plan shape doesn't break the whole card render. **UAT**: **11 new v4.81.21 assertions** — 5 wiring (renderTodayPlan reads bridges + dedupes + emits container + emits buttons + onclick handler), 4 CSS structural (.tplan-bridges + .tplan-bridge-btn + reduced-motion + mobile breakpoint), **2 vm-fixture behavioral tests** — (a) plan with subnet topics → exactly ONE deduped bridge button rendered (Subnetting + IPv6 share kind='subnet' so dedupe correctly collapses them); (b) plan with NO subnet topics → no bridge container or buttons rendered (no orphan markup). Total UAT: 4622 → **4633** passing. Playwright: **79/79** still green. Cache: `netplus-v4.81.21`. ~85 LOC across `app.js` (bridge composition logic) + `styles.css` (.tplan-bridges + .tplan-bridge-btn block) + `tests/uat.js` (11 new). **101st ship**. **Sunday's 16th ship**. Closes the v4.81.18 follow-up backlog completely. The Today's Plan card now matches v4.43.1 + v4.81.18 + v4.81.21 affordance baseline: signal-coded chips for individual drills, Subnet Trainer bridge for math-deep topics, Begin plan CTA for the multi-topic session — three layers of secondary-to-primary affordance. |
| v4.81.20 | **🧹 Retire orphan v4.54.0 `#focus-banner` (user dogfood after v4.81.18 ship)**. User screenshot post-v4.81.18 deploy: *"not sure if this was the intention but this is how the card looks by the way"* — showed the OLD prescriptive banner rendering with stale "Seven questions per topic, mixed difficulty. Your fastest route to exam-ready. Focus: WAN Connectivity and Network Troubleshooting Methodology." text. Investigation revealed: the v4.81.18 consolidation correctly handled the 3 surfaces INSIDE `#today-section` (`#todays-focus`, `#rotation-row`, `#session-banner`) but missed the v4.54.0 `#focus-banner` which sits OUTSIDE the today-section as a SIBLING. The new `#today-plan` was correctly hidden by the `isStudyPlanDoneToday()` gate (user had already run a session today), but the focus-banner doesn't share that gate, so it kept showing the old prescriptive copy. **Audit miss diagnosis**: when consolidating "today section", I scoped my search to `#today-section` children. Should have also grepped for any other "today's plan" / "study plan" prescriptive surface in the home page render path. Lesson captured for future consolidation work — when retiring redundant surfaces, scope the search to all visible elements on the relevant page, not just the named container. **Fix**: `renderSetupFocusBanner()` rewritten as a thin compat shim that adds `is-hidden` to `#focus-banner` + clears its innerHTML + delegates to `renderTodayPlan()`. The element stays in the DOM (UAT regression guards check for its existence) but is permanently invisible. The full v4.54.0 in-banner content (greeting copy + weak-spot pullquote + "Seven questions per topic..." prose + warmup CTA + focus CTA) is gone — ~70 lines of dead code retired. Now the canonical `#today-plan` card (with `isStudyPlanDoneToday()` gate, signal-coded chips, footer composition row) is the SOLE prescriptive surface. **Done shape**: when the consolidated card is visible (active plan, not yet done today) → user sees ONE card. When the consolidated card is hidden (plan done today) → user sees neither banner — the homepage cleanly indicates "you're done for today" instead of having an orphan banner pretending the plan is still active. **CSS retained**: all v4.54.0 `.focus-banner-v2` + `.fb-*` classes kept intact (the element is still in the DOM, just hidden — keeps any external selectors valid). **UAT**: **9 new v4.81.20 assertions** — 4 retire-shape (shim < 800 chars + delegates to renderTodayPlan + hides element + clears innerHTML), 3 stale-content tombstones (Seven-questions-per-topic copy gone from app.js / fastest-route copy only in renderTodayPlan / renderSetupFocusBanner doesn't reference computeWeakSpotScores anymore), 2 HTML/CSS structural (#focus-banner stays in DOM as compat shim + .focus-banner-v2 CSS retained), **1 vm-fixture behavioral test** — stubs DOM + renderTodayPlan, runs the shim, asserts is-hidden added + innerHTML cleared + renderTodayPlan delegated exactly once. Plus 4 existing v4.53.0 + v4.54.0 tests converted to tombstones (greeting copy + computeWeakSpotScores reference + fb-quote/fb-body/fb-cta children all asserted GONE from the shim). Total UAT: 4612 → **4622** passing. Playwright: **79/79** still green. Cache: `netplus-v4.81.20`. ~75 LOC removed (legacy banner content) + ~25 LOC added (shim) net **~50 LOC reduction** across `app.js` + `tests/uat.js` (9 new + 4 retargeted tests). **100th ship**. **Sunday's 15th ship**. Closes the dogfood-surfaced "two prescriptive banners" gap discovered after v4.81.18. Audit-pattern lesson: when retiring redundant surfaces, scope the grep beyond just the named container. |
| v4.81.19 | **🔧 Comma-safe `Multi:` topic parser (pre-existing bug spotted during v4.81.17 Domain Drill ship)**. 3 topic names contain literal commas: `'NTP, ICMP & Traffic Types'`, `'SDN, NFV & Automation'`, `'Firewalls, DMZ & Security Zones'`. All 4 sites that parse the `'Multi: <topic, topic, ...>'` sentinel used naive `.split(',')`, mis-segmenting those topics into separate items (e.g. "NTP" + "ICMP & Traffic Types" instead of the canonical name). Bug got more visible with v4.81.17 Domain Drill (3 of 5 domain presets contain a comma-named topic) — Haiku was being told to write questions matching mis-split topic strings, history filtering missed them, multi-count over-reported. Mostly cosmetic in practice (Haiku still produces reasonable questions; per-topic history saves via `q.topic` not the multi-list) but worth fixing for correctness. **Fix**: new `_parseMultiTopicSentinel(qTopic)` helper near `TOPIC_DOMAINS` definition does greedy longest-match against `Object.keys(TOPIC_DOMAINS)` sorted by length descending, so multi-word topic names with commas are preserved verbatim. Accepts both full sentinel (`'Multi: A, B'`) and bare body (`'A, B'`). Falls back to the legacy naive split when `TOPIC_DOMAINS` isn't defined (defensive — should never happen in practice). All 4 split sites updated: (a) `_pickExemplarsForTopic` first-topic extraction for prompt-injection exemplar pick, (b) `_filterHistoryByTopic` multi-list `.includes(topic)` check (drives Progress page topic-rollup + Study Plan ranking), (c) `startQuiz`'s `_multiCount` (display count for the loading message), (d) `_fetchQuestionsBatch` Multi: distribution prompt (highest-impact — feeds Haiku the canonical topic list it must match `.topic` field against). Each call site keeps a `(typeof _parseMultiTopicSentinel === 'function' ? ... : <legacy>)` ternary so the legacy parser stays as a defensive fallback if the helper is somehow unavailable. **Discovery during ship**: user dogfood screenshot revealed that the v4.81.18 Today consolidation missed retiring the v4.54.0 `#focus-banner` (a separate prescriptive surface OUTSIDE the today-section) — it's still rendering "Seven questions per topic, mixed difficulty. Your fastest route to exam-ready" via `renderSetupFocusBanner()`. The new `#today-plan` is correctly hidden when `isStudyPlanDoneToday()` returns true (post-session today), but the focus-banner doesn't share that gate. Filed as v4.81.20 follow-up — same audit pattern as v4.81.18 but extended to the orphan banner. **UAT**: **11 new v4.81.19 assertions** — 4 helper structure (function defined + sort-by-length-desc + accepts-both-shapes + fallback-defense), 4 wiring (4 call sites use the helper), **3 vm-fixture behavioral tests** — (a) all 3 comma-containing topics in one Multi: sentinel parsed verbatim alongside non-comma topics (5 topics in, 5 out, all preserved); (b) regression vs naive split — naive over-segments to 3 pieces and chops "NTP" off the front, new parser keeps "NTP, ICMP & Traffic Types" intact (2 pieces total); (c) parser accepts full sentinel + bare body equivalently + handles empty/null defensively. Plus 2 existing structural-regex windows widened (v4.57.4 200→500/100→300; v4.58.0 100→250) to accommodate the new ternary fallback wrapper. Total UAT: 4601 → **4612** passing. Playwright: **79/79** still green. Cache: `netplus-v4.81.19`. ~80 LOC across `app.js` (helper + 4 call site updates) + `tests/uat.js` (11 new + 2 widened). **99th ship**. **Sunday's 14th ship**. Closes the comma-split correctness gap; sets up v4.81.20 (focus-banner retirement). |
| v4.81.18 | **🎯 Today section consolidation — 3 stacked surfaces → 1 prescriptive card (user dogfood)**. User screenshot of the Today section called out *"theres alot going on... feels like there aint uniform order... maybe some stuff here is redundant."* Honest audit confirmed: (1) Topic names appeared **twice** (Weak Spots chip row + Study Plan banner restated them); (2) "never studied" meta repeated **5 times** in the rotation row; (3) **Three competing CTAs** (weak chip click / rotation chip click / Begin plan) violated Codex r7-r9's "every page has one strong recommendation" pattern; (4) Three visual languages stacked (purple chips + amber chips + purple gradient banner); (5) Plan ignored the rotation signal entirely (5 stale topics shown, plan focused only on 2 weak topics). **Discipline win**: followed `feedback_concept_mockup_first.md` — drafted a standalone HTML mockup (`mockups/today-consolidation-concept.html`) showing before/after + 3 design rationale annotations + savings tally + implementation notes. User approved the mockup verbatim → coded the live version directly from it. Zero revision rounds. **The fix — single `#today-plan` card** with: eyebrow ("Today's plan"), headline ("5 topics · ~21 min · *fastest* route to exam-ready"), prose sub-line tailored to which signals contributed ("Hits your 2 weakest topics from recent quizzes plus 3 topics you haven't touched in a while"), inline color-coded chip strip (🎯 yellow border = weak, 🕒 amber border = stale), footer composition row ("2 weak · 3 stale · Mixed difficulty" — explicitly answers "which 5 topics?"), one primary "Begin plan →" CTA. Chip click still drills the topic individually (secondary affordance preserved); primary CTA still calls existing `startSession()` which now uses the rewritten `buildSessionPlan` so the executed plan matches what the card shows. **Architecture**: rewrote `buildSessionPlan(n)` to compose top 2 weak (from `computeWeakSpotScores`) + top 3 stale (from `_computeStaleTopics`) with dedupe between sources. Falls back to the legacy `_scoreTopicNeed` ranker when both signals are sparse (brand-new users with no history) so the plan stays non-empty in early-stage usage. Schema preserved (`{topic, reason, color}` plus new `signal` + `meta` fields) so all 4 existing consumers (`renderTodayPlan`, `startSession`, `isStudyPlanDoneToday`, `runSessionStep`) work unchanged. Bumped `SESSION_TOPICS = 3 → 5` to match the new composition. **Compat shims**: `renderTodaysFocus()`, `renderRotationChips()`, `renderSessionBanner()` are now thin delegators that hide their legacy DOM element + call `renderTodayPlan()`. The legacy elements (`#todays-focus`, `#rotation-row`, `#session-banner`) stay in the DOM as permanently-hidden compat shims so any UAT regression guard or external code that grabs them by ID doesn't crash. CSS for the legacy classes also retained. **Retired affordances** (filed as follow-ups): (a) v4.42.0 FLIP rerank animation in renderTodaysFocus — was specific to in-place chip reordering; doesn't apply to the new card which renders fresh chips each call. (b) v4.43.1 Subnet Trainer bridge link in renderTodaysFocus — affected ~3 subnet-heavy topics (Subnetting & IP Addressing / IPv6 / NAT & IP Services). `WEAK_SPOT_DRILL_BRIDGES` + `openWeakSpotBridge` constants kept (still used by Subnet Trainer dashboard). Bridge re-integration into the new card spawned as a separate task. **Mobile**: `<540px` breakpoint stacks the foot row vertically + shrinks the eyebrow indent. Reduced-motion gate neutralizes hover lift + active scale on chips and CTA. **UAT**: **19 new v4.81.18 assertions** — 5 helper/constant existence (renderTodayPlan + 2 constants + SESSION_TOPICS bump + buildSessionPlan rewrite signature), 6 wiring (compat shim delegations + isStudyPlanDoneToday gate + buildSessionPlan call + chip-strip + foot row + CTA + signal data attribute), 4 HTML/CSS structural (#today-plan element + is-hidden default + .today-plan + .tplan-chip variants + .tplan-cta + mobile breakpoint + reduced-motion gate), **2 vm-fixture behavioral tests** — (a) `buildSessionPlan(5)` composes 2 weak + 3 stale in correct order with stub data, with overflow weak/stale topics correctly excluded; (b) dedupe handles overlap between weak + stale signals (IPv6 in both → appears exactly once as 'weak', plan tops up via fallback ranker to maintain 5 unique topics). Plus 9 retargeted/retired tests for legacy renderTodaysFocus assertions (FLIP machinery + bridge logic) — converted to tombstones asserting the behaviour is gone from the shim and not re-introduced in renderTodayPlan. Total UAT: 4582 → **4601** passing. Playwright: **79/79** still green. Cache: `netplus-v4.81.18`. ~310 LOC across `app.js` (renderTodayPlan + buildSessionPlan rewrite + 3 compat shims + 1 constant block) + `index.html` (1 new element, 3 element comments updated) + `styles.css` (new `.today-plan` + `.tplan-*` block) + `tests/uat.js` (19 new + 9 updated). **98th ship**. **Sunday's 13th ship**. Closes the &ldquo;3 competing CTAs&rdquo; gap on the homepage; pattern reusable for any future stacked-surface consolidation. |
| v4.81.17 | **🎯 Domain Drill — one-click quizzing on a single N10-009 domain (user request)**. User dump: *"can we also have the ability to pick a domain and test ourselves on that domain and select difficulty and questions level as per usual? currently to test yourself in a domain you have to select all the domain topics which can be tedious."* The friction was real — to drill Domain 1.0 Networking Concepts you had to expand the accordion + click 13 individual topic chips. **Two-surface fix** (user picked Option A — both): **(1) Mode Ladder secondary row** in the Practice tier — 5 color-coded domain tiles (1.0 Concepts purple / 2.0 Implementation green / 3.0 Operations blue / 4.0 Security orange / 5.0 Troubleshooting red, matching the existing accordion + Progress page borders so visual language is consistent across all 3 surfaces). Click a tile → `applyDomainPreset(key)` fires immediately: builds `topic = 'Multi: <all topics in domain>'`, sets `diff = 'Exam Level'`, `qCount = 10`, syncs chip state, calls `startQuiz()`. One-click, ~8-min focused drill. **(2) Custom Quiz pre-fill pill row** above the topic accordion — 5 compact pills "1.0 Concepts" / "2.0 Implementation" / etc. Click → `prefillDomainTopics(key)` selects all the domain's chips, deselects mode cards (Smart/Mixed contradict explicit domain selection), opens the matching accordion, refreshes the CQ summary bar, scrolls to the topic group. Does NOT start the quiz — user picks diff + count + Generate as usual. Power-user path for "30 Q Hard on Domain 4.0 Security" without manual chip toggling. **Why both, not one or the other**: Mode Ladder alone leaves users stuck at 10 Q Exam Level; Custom Quiz pre-fill alone keeps the feature buried behind the `<details>` collapsible. Together: Mode Ladder = fast path, Custom Quiz = flexible path. Both reuse the same multi-topic infrastructure — `'Multi: ...'` sentinel + existing chip-state sync + existing `startQuiz` pipeline. No new fetch path, no new validation surface, no new state. **Helpers**: `_topicsInDomain(key)` reverse-lookup with memoised `_DOMAIN_TOPICS_CACHE` (lazy-built once on first call, walks `TOPIC_DOMAINS` in O(50)); `_DOMAIN_IDX` maps domain keys to 1-5 for accordion-open targeting. **Visual design**: tiles are 5-column grid on desktop, collapse to 2-column at <720px, single-column at <420px. Reduced-motion gate neutralizes the hover lift + active scale. **Scope-of-solution discipline win**: surfaced both "where does this live" + "preset vs customizable" decisions up front (mockup-first wasn't needed — the UI shape was clear). The user's wrestle ("Mode Ladder OR Custom Quiz?") got answered with "both, but with different roles" — same pattern as `feedback_scope_disambiguation.md` taught (when wrestling, the answer is often "both surfaces, different intents"). **Pre-existing bug spotted + filed**: 3 topic names contain commas (`"NTP, ICMP & Traffic Types"`, `"SDN, NFV & Automation"`, `"Firewalls, DMZ & Security Zones"`) and the 4 `Multi:`-parsing sites all use `.split(',')` — would mis-split those topics. Mostly cosmetic in practice (Haiku still produces reasonable questions; per-topic history saves via `q.topic` not the multi-list). Filed as a separate ship to fix with greedy-match-against-catalog parser. **UAT**: **25 new v4.81.17 assertions** — 11 helper/wiring (3 fns + 2 caches/maps + signature/state/start checks across applyDomainPreset + prefillDomainTopics), 6 HTML structural (Mode Ladder row + 5 tiles + 5 pre-fill pills + 5 onclick handlers), 4 CSS structural (tiles + pills + color-coded borders + mobile breakpoint + reduced-motion gate), **2 vm-fixture behavioral tests** — (a) `applyDomainPreset` runs across all 5 domain keys and verifies 6 invariants per call (Multi sentinel, all in-domain topics included as substrings, no out-of-domain topics leaked as standalone entries — accommodates the 3 comma-containing topics by checking substring presence with bracketing rather than naive split, diff=Exam Level, qCount=10, startQuiz fired exactly once); (b) `_topicsInDomain` memoisation works (same array reference returned on second call) + all 5 domains have ≥5 topics + unknown key returns empty array. Plus a regex update to the v4.50.0 accordion-count test (scoped to `<details class="topic-domain-group">` matches only) since data-domain-idx now appears on 15 elements (5 accordions + 5 tiles + 5 pills) instead of 5. Total UAT: 4557 → **4582** passing. Playwright: **79/79** still green. Cache: `netplus-v4.81.17`. ~280 LOC across `app.js` (helpers + handlers) + `index.html` (Mode Ladder row + Custom Quiz pre-fill row) + `styles.css` (tiles + pills + color-coding + mobile + reduced-motion) + `tests/uat.js` (25 assertions). **97th ship**. **Sunday's 12th ship**. Domain drilling is now one-click instead of 7-13 chip toggles. Pattern reusable for any future "select a group of topics by category" workflow. |
| v4.81.16 | **🐛 Question-quality bugfix — "Which TWO" Wi-Fi channels slip-through (user dogfood after v4.81.15 ship)**. User screenshot during a Foundational quiz: a multi-select Q5 asked *"Which TWO of the following are non-overlapping frequency channels in the 2.4 GHz Wi-Fi band..."* — pedagogically wrong because the canonical N10-009 fact is the 1/6/11 **trio**, not a pair. Submit was disabled with "Select 2 answers" forcing the user to pick fewer than the right answer. **Two failure modes converged**: (a) Haiku produced a "Which TWO" stem when the only fact-aligned correct answer is "Which THREE = 1, 6, 11", (b) `_groundTruthOk` early-returned on `getQType(q) !== 'mcq'` so multi-select questions skipped GT entirely. Even with `RETENTION_GAP_CONCEPTS` flagging "WAP Channels" + an exemplar at app.js:1417 teaching this exact fact, neither layer **enforced** it on output. **Two-layer fix**: **Layer A — generic stem-vs-answer-count guard** (`_stemNumericMatchesAnswerCount`). Tight regex `(?:\bWhich\s+\|\(\s*Choose\s+\|^\s*Choose\s+)(TWO\|THREE\|FOUR\|FIVE\|two\|three\|four\|five\|[2-5])\b` matches "Which TWO" / "(Choose TWO)" / "Choose TWO" patterns only — deliberately tight to avoid false-positives on prose like "Pick two factor authentication" (verbs `Identify`/`Pick` excluded). For multi-select: asserts `q.answers.length === expectedCount`. For MCQ: rejects outright (an MCQ stem demanding multiple is type-mismatched on its face). Pass-through for `order` / `cli-sim` / `topology`. **Layer B — multi-select GT facts** (`_multiSelectGroundTruthOk`). Locks the 2.4 GHz canonical fact: stem signal `non-overlapp` + `channel` + (`2.4 GHz` OR `Wi-Fi`/`wireless`/`802.11`/`wlan`) → marked answers must extract to exactly the set `{1, 6, 11}` (sorted-equality check on `Number.isFinite` channel numbers). If not, reject. Designed as a table — future canonical multi-select facts (WPA3 ciphers, CIA properties, etc.) join the same function with their own stem-detection regex + canonical answer set. Wired into `validateQuestions`: stem-numeric guard runs at the top of the per-question filter (works for all qTypes); multi-select GT runs in the multi-select branch alongside existing structural validity. **Validation-audit harness updated** to extract the 2 new helpers + the `_STEM_NUMBER_WORDS` const block — without that, the audit subprocess hits `ReferenceError` because `validateQuestions` calls helpers that aren't in the temp-stub bundle. Always required when a new helper joins the validator pipeline. **UAT**: **10 new v4.81.16 assertions** — 8 structural (2 helper-existence + 1 const-existence + 2 wiring + 1 canonical-fact literal + 1 regex coverage + 1 false-positive-avoidance regression guard ensuring `Identify`/`Pick` verbs are NOT in the regex), **2 vm-fixture behavioral tests** — (a) the user-reported bug case ("Which TWO" + 3 answers) returns false, while a correct "(Choose TWO)" + 2 answers returns true, a no-explicit-count multi-select returns true (pass-through), and a MCQ stem with "Which TWO" returns false (type-mismatch); (b) the GT check rejects the bug case ({1,6} only), accepts the canonical {1,6,11}, rejects a wrong-set ({1,6,9}), and passes through unrelated multi-selects (e.g. "Which TWO ports does FTP use" with answers {20,21}). Validation audit still 64.7% catch / 0% FP after the additions — the two new layers add new rejection paths without false-positive any of the 23 broken-corpus or curated-good fixtures. Total UAT: 4547 → **4557** passing. Playwright: **79/79** still green. Cache: `netplus-v4.81.16`. ~150 LOC across `app.js` + `tests/uat.js` + `tests/validation-audit.js`. **96th ship**. **Sunday's 11th ship** — first same-day bugfix follow-up of the day, surfaced by user dogfooding immediately after the v4.81.15 stale-topic ship landed. Pattern repeats `feedback_dogfooding_beats_scanners.md`: real use catches what scanners miss. Future canonical multi-select facts can extend `_multiSelectGroundTruthOk` without touching MCQ paths. |
| v4.81.15 | **🕒 Stale-topic surfacing — 4-layer rotation algorithm (user request)**. User dump: *"across the app, theres gotta be something that monitors which topics haven't been touched yet in a while during setting up mixed question or exam simulator. maybe some kind of algorithm that make sure the user is addressing some topics that haven't been seen in a long while."* Investigation confirmed the gap: `computeWeakSpotScores()` already calculates per-topic staleness for the homepage Weak Spots widget, but that signal never feeds back into the question generator. `RETENTION_GAP_CONCEPTS` (v4.62.3) covers MISSED concepts as a soft tiebreaker, but not STALE (untouched) topics. After brainstorm, user picked **Layers 1+2+3+5** (deferred Layers 4 calendar-aware + 6 exam-coverage tracking as separate considerations). **(1) Base stale-topic injection** — new `_computeStaleTopics(historyRows, n, sliceIdx, sliceSize)` helper walks `TOPIC_DOMAINS` (50 topics) and combines `computeWeakSpotScores`'s posterior accuracy + `loadHistory`'s last-seen timestamps. Topics not seen in `WEAK_STALENESS_DAYS` (14d) become eligible; never-studied topics are infinitely stale (9999d sentinel). `_formatStaleTopicsForPrompt(staleTopics)` mirrors `_formatRetentionConceptsForPrompt`'s soft-preference framing: *"ROTATION PRIORITY: stay within the blueprint weights, but bias topic selection within each domain toward this list when reasonable"* — never a mandate. Injected after `${retentionBlock}` in `_fetchQuestionsBatch`'s `buildPrompt` template. Gated to MIXED_TOPIC only (single-topic + multi-topic quizzes already focus the user's explicit selection). **(2) Compound priority (the smart-rotation amplifier)** — `priority = daysSince × (1 + accGap)` where `accGap = max(0, WEAK_TARGET_ACC − posterior)`. A topic last seen 21d ago at 50% accuracy outranks one last seen 28d ago at 90% accuracy: B has more raw stale days, but A has more learning value to reclaim. Verified by vm fixture (`A_StaleAndWeak` 21d/50% MUST outrank `B_StaleButStrong` 28d/90% on priority — passes deterministically). **(3) Per-batch slice rotation (the exam-breadth amplifier)** — `_computeStaleTopics(history, n, sliceIdx, sliceSize)` returns a rotating window with step = `floor(sliceSize/2)` (50% overlap between consecutive slices for coverage continuity). `fetchQuestions` accepts an optional 5th parameter `staleSliceIdx` and threads it through to `_fetchQuestionsBatch`. `startExam` passes `i` (0..4) for each of the 5 sequential exam batches → each batch sees a *different* rotating window of stale topics, systematically covering MORE stale ground across a 90-Q exam instead of all 5 batches clustering at the same top-N. Marathon mode's batch loop also passes `i` for the same effect on bulk30/45/60. Within a single `fetchQuestions` parallel-batch case, sub-batches get `outerIdx + i` so even concurrent sub-batches see overlapping but non-identical windows. Retry-to-fill threads the same staleSliceIdx as the parent batch (so we keep filling the same rotation window). **(5) Homepage "Due for rotation" chip row** — closes the UX loop so the user can SEE the algorithm working. New `#rotation-row` element directly below `#todays-focus` (Weak Spots) on the home page. `renderRotationChips()` renders amber-tinted chips (visually distinct from accent-tinted Weak Spots — softer "FYI" treatment vs. urgent weak-spots) with topic name + meta strip ("21d stale · ~55% acc" or "never studied"). Click-to-drill via existing `focusTopic()`. Hidden when `loadHistory().length < STALE_CHIP_MIN_HISTORY` (5) so new users don't see a row of 50 stale topics they haven't studied yet — give the baseline time to build. Same dual-wiring discipline as v4.81.1: rendered from BOTH `goSetup()` (post-navigation refreshes) AND `DOMContentLoaded` (initial paint) so the chip row state is consistent on raw reload. **4 new constants**: `STALE_PROMPT_TOPIC_COUNT = 10` (injected per batch), `STALE_PROMPT_SLICE_SIZE = 10` (rotation window), `STALE_CHIP_TOPIC_COUNT = 5` (homepage), `STALE_CHIP_MIN_HISTORY = 5` (gate for both prompt + chip row). **Why this bundle**: #1 + #2 are the algorithmic core (without #2 it's a dumb timer; without #1 nothing has anything to bias). #3 unlocks exam breadth which is where the dogfood signal originated. #5 makes the invisible visible — same pattern as v4.81.13's domain breakdown card. Layers #4 (calendar-aware threshold tightening) and #6 (exam-coverage tracking) deferred as speculative without dogfood telemetry. **UAT**: **33 new v4.81.15 assertions** — 7 helper/constant existence (3 fns + 4 constants), 7 wiring (signature changes + propagation through fetchQuestions + _fetchQuestionsBatch + startExam main + retry + Marathon + parallel sub-batches), 5 prompt content (compound priority formula + ROTATION PRIORITY framing + blueprint-weight discipline + MIXED_TOPIC gate + STALE_CHIP_MIN_HISTORY gate + 9999 never-studied sentinel + reuses computeWeakSpotScores), 4 UI structural (HTML element + is-hidden default + .rotation-row + .rot-chip CSS + reduced-motion gate), 4 lifecycle wiring (renderRotationChips in goSetup + DOMContentLoaded), **3 vm-fixture behavioral tests** — (a) compound priority correctly ranks stale+weak above stale+mastered using deterministic stub `computeWeakSpotScores` (A_StaleAndWeak 21d/50% beats B_StaleButStrong 28d/90% by priority despite fewer raw stale days; D_NeverStudied wins overall on 9999-day staleness; C_Fresh < 14d filtered out), (b) slice rotation produces distinct windows across batches (sliceIdx 0 = T1..T4, sliceIdx 1 = T5..T8, sliceIdx 2 wraps modular back to T1..T4 — proves both rotation + wrap), (c) prompt formatter handles never-studied vs stale-with-history distinctly (never-studied uses sentinel string, doesn't leak the 9999d number into the prompt). Plus 6 existing structural-regex widenings to accept the new optional `staleSliceIdx` parameter (APP_VERSION + 5 fetchQuestions/startExam/Marathon callsites). Total UAT: 4514 → **4547** passing. Playwright: **79/79** still green. Cache: `netplus-v4.81.15`. ~330 LOC across `app.js` (helper + format + render fn + 5 callsite threadings) + `index.html` (1 element) + `styles.css` (1 chip-row block) + `tests/uat.js` (33 assertions). **95th ship**. **Sunday's 10th ship**. Closes the long-standing "no rotation algorithm" gap — the system now systematically surfaces stale-AND-weak topics into mixed quizzes + exam batches via prompt injection AND tells the user about it via a visible chip row. Layers 4 (calendar-aware) and 6 (exam-coverage tracking) remain on standby until dogfood telemetry justifies. |
| v4.81.14 | **🔁 Cross-batch question dedup (user-reported repeats)**. User report: *"some questions kept repeating themselves lol... in the exam simulator. also this happens in regular quizzes sometimes too but i noticed it a lot more in the exam simulator."* Investigation confirmed zero dedup anywhere in the question-fetch pipeline. Two failure modes: (1) `fetchQuestions` for n>10 splits into parallel `_fetchQuestionsBatch` calls via `Promise.allSettled` — concurrent batches can't see each other's output, so Haiku produces overlapping stems independently. (2) `startExam` calls `fetchQuestions` 5 times sequentially with no cross-call dedup; Haiku has no state from prior batches. **Fix**: stem-normalize-and-dedupe at two levels. **`_normalizeStemForDedup(stem)`**: lowercase + strip punctuation (`/[^\\w\\s]/g`) + collapse whitespace + 200-char cap. Catches *"Which of the following BEST describes ARP?"* and *"which of the following best describes arp."* as the same stem. **`fetchQuestions` parallel-batch merge**: Set-based first-seen-wins dedup as the merge step iterates `Promise.allSettled` results. Logs `deduped` count via `console.info` when > 0. **`startExam` per-batch loop**: before each batch concats into `examQuestions`, filter out any stem already in the running set. Plus the retry-to-fill payload also gets deduped (using a fresh seen-stem Set built from `examQuestions` + the current batch's already-deduped contents) — otherwise the retry would undo the dedup. Order matters: dedup happens BEFORE the existing retry-to-fill logic, so deficits from dropped duplicates auto-trigger a refill. No new failure modes — the existing DROPOUT_BUFFER + retry-to-fill cascade handles the shortfall. **UAT**: **8 new v4.81.14 assertions** — 1 helper exists + 1 helper logic check + 2 fetchQuestions wiring (Set + helper used; deduped count logged) + 2 startExam wiring (cross-call dedup against examQuestions; retry-to-fill uses seenStemsRetry too) + **2 vm-fixture behavioral tests** (normalization collapses case/punct/whitespace/nullish to same key; merge logic across 3 parallel batches with 2 deliberate overlaps verifies first-seen-wins keeps original case + drops duplicates). Total UAT: 4506 → **4514** passing. Playwright: **79/79** still green. Cache: `netplus-v4.81.14`. ~110 LOC across `app.js` + `tests/uat.js`. **94th ship**. **Sunday's 9th ship**. Closes the question-repetition gap that affected exam mode most heavily (5 sequential batches × 18 Qs = 90 chances for cross-batch overlap) but also occasionally surfaced in larger regular quizzes. |
| v4.81.13 | **🎯 Exam → readiness/progress/analytics integration + per-domain breakdown (user request)**. User report after first exam (780/900): *"ideally this should reflect somehow on the exam readiness score… rather than just a standalone entity and it should reflect somehow in the progress and analytics part for what topics i got right/wrong and their domains."* Investigation confirmed the gap: `submitExam` saved ONE summary row tagged `topic: EXAM_TOPIC`, and `getReadinessScore` explicitly filters EXAM_TOPIC OUT — so a 90-question exam contributed **zero signal** to readiness/progress/analytics/weak-spots/what-if. Wrong-bank + SR queue worked fine, but every headline metric ignored the exam entirely. **Two fixes (user picked A+C)**: **(A) Per-topic history split** — `_saveExamPerTopicSplit(log, hardcore)` called from submitExam after the summary saveToHistory. Groups `log[]` by `q.topic` and writes one history row per topic, tagged `mode: 'exam'` (1.3× boost applies via existing buildWeightedTopicMap math) and `via: 'exam-split'` (traceable). Skips MIXED_TOPIC + EXAM_TOPIC + empty topic entries defensively. No double-counting because getReadinessScore already filters the EXAM_TOPIC summary. A 90-Q exam covering ~12-15 topics → ~12-15 new history rows that flow into readiness/progress/weak-spots immediately. **(C) Per-domain breakdown card on exam-results page** — `_buildExamDomainBreakdown(log)` produces `{ domainKey: {correct, total, pct, tier} }` for all 5 N10-009 domains using v4.45.1 anchored thresholds (mastered ≥85, proficient ≥70, developing ≥55, novice <55, empty for zero questions). `renderExamDomainBreakdown(log)` paints the card on the exam-results page in CompTIA blueprint order. Color-coded tier per row. User immediately sees "I got 9/12 in Concepts, 8/10 in Implementation, 4/9 in Troubleshooting" right after submit. **Limitation**: forward-only — the user's existing 780 won't retroactively populate (we don't have per-question data for past exams). Next exam benefits fully. **UAT**: **14 new v4.81.13 assertions** — 3 helper-existence + 2 wiring + 3 logic checks (via marker, mode marker, defensive skips) + 1 tier-threshold + 2 HTML/CSS + **3 vm-fixture behavioral tests** (per-topic split writes one row per unique topic with correct/total math; defensive skips for MIXED/EXAM/empty; domain breakdown buckets correct + tier per domain). Plus widened the v4.54.17 submitExam → _maybeShowDailyRecap regex 400→1500 for the new helper calls. Total UAT: 4491 → **4506** passing. Playwright: **79/79** still green. Cache: `netplus-v4.81.13`. ~250 LOC across `app.js` + `index.html` + `styles.css` + `tests/uat.js`. **93rd ship**. **Sunday's 8th ship**. Closes the long-standing "exam is a standalone entity" gap. |
| v4.81.12 | **🎛️ Settings Control Centre — 4-section reorg (Codex r9 #6 / Issue #266)**. Closes the Codex r9 cycle. v4.81.11 shipped 4 of 6 polish items; #266 was the structural reorg framing Settings as a deliberate trust/control centre. **Layout**: settings-layout now contains 4 `<div class="settings-group">` containers, each with a `.settings-group-head` (§-numbered eyebrow + display heading + sub copy) above its cards. **§01 Study Setup**: Health card + Exam Date + Daily Goal. **§02 AI Coach**: API Key. **§03 Data & Backups**: Manual export/import + Automatic backups. **§04 Danger Zone** (visually distinct, red-tinted): Clear Wrong Answers Bank. Red treatment: `rgba(248,113,113,.04)` background + 18% red border + red eyebrow + red section-title color + red "zone" accent in heading. **Critical discipline**: zero changes to destructive function bodies (clearWrongBank, restoreFromAutoBackup, importData). All existing IDs preserved exactly so existing JS handlers + v4.81.x UAT keep working. The reorg is purely a wrapping-DOM transformation — used a Python script to slice the inner settings-layout block + rewrite once instead of multiple Edit calls (cleaner + atomic). Also fixed cosmetic bug: existing `\\u2014` literal escape sequences in the Exam Date hint became proper `&mdash;` HTML entities. **UAT**: **12 new v4.81.12 structural assertions** — 4 group containers exist, 4 §-numbered eyebrows present, Danger Zone has dedicated class, Wrong Answers section has danger class, each section ID lives within its expected group's `data-group` scope (Health → study-setup, API key → ai-coach, Export+Import + Auto-backups → data-backups, Wrong Answers → danger-zone), .settings-group CSS declared, Danger Zone has red tint, mobile breakpoint exists. Plus widened the v4.54.1 settings-shape regex window 5000→7000 (page-settings → exportData) and 2500→3500 (importData → clearWrongBank) for the new ordering. Total UAT: 4479 → **4491** passing. Playwright: **79/79** still green. Cache: `netplus-v4.81.12`. ~280 LOC across `index.html` + `styles.css` + `tests/uat.js`. **92nd ship**. **Sunday's 7th ship** — closes the Sunday cycle at 7 ships, 0 same-day bugfix follow-ups across the entire day. **Codex r9 cycle closed at 5 of 6 shipped** (#265 BYOK is `saas-gated`, deferred). Live Chrome MCP verification was attempted but blocked on a permission prompt timeout — proceeded on UAT + Playwright confidence given zero changes to destructive function bodies (only markup wrapping). |
| v4.81.11 | **🛡️ Settings trust+safety polish (Codex r9 — 4 of 6)**. Codex r9 reviewed Settings, rated 7.6/10 (lowest yet) — *"utility drawer, not yet SaaS-grade trust/control centre"*. Shipped 4 focused improvements; deferred Control Centre reorg (#266) + BYOK server-side API key (#265, `saas-gated`). **(2) Import button a11y**: pre-fix the Import action was a `<label>` wrapping a hidden `<input type="file">` — not a real focusable control. Now uses a real `<button>` + adjacent hidden `<input id="import-file-input">` pattern: button onclick triggers `document.getElementById('import-file-input').click()`. Proper focus state, keyboard accessible, screen-reader friendly. **(3) Danger confirmations**: `clearWrongBank()` confirm now lists exactly what gets deleted + the recovery path. **(4) Restore "what will change" copy**: `restoreFromAutoBackup()` confirm enumerates the data categories that get replaced (Quiz history + streak + readiness · Daily goal + exam date · Wrong bank + SR queue · All drill mastery · Topology saves + ACL state · API key, milestones, type stats). **(5) Study Setup Health card**: new top-of-page status panel — at-a-glance read-only summary populated by `renderSettingsHealthCard()` on every Settings open. 5 rows: API key status · Exam date · Daily goal · Auto-backup · Today's progress. Three status tiers: ok (green) / mid (orange) / warn (yellow). **UAT**: **12 new v4.81.11 assertions** — 4 for Import a11y (real button + hidden input + click delegation + regression guard against the old `<label>` wrap), 1 each for clearWrongBank + restore confirm enumerations, 5 for Health card (function exists + renderSettings hook + DOM container + 5-row coverage + CSS tiers), **1 vm-fixture behavioral test** that simulates `renderSettingsHealthCard()` with/without an API key and asserts the rendered HTML contains "Not connected" → "Connected · sk-ant-…" transition. Total UAT: 4466 → **4479** passing. Playwright: **79/79** still green. Cache: `netplus-v4.81.11`. ~280 LOC across `app.js` + `index.html` + `styles.css` + `tests/uat.js`. **91st ship**. **Sunday's 6th ship** — closes Sunday with clean cadence (5 Codex-driven ships across r5-r9 + 1 standalone polish, 0 same-day bugfix follow-ups). |
| v4.81.10 | **🐛 + 🎯 Drill bugfix + Mission Cards (Codex r8)**. Codex r8 reviewed all 5 drills, rated 8.4/10, flagged a real bug + recommended a Drill Mission Card pattern. **(1) Bug fix**: Codex flagged *"Start Lesson 1 buttons did not visibly transition into lesson content"*. Root cause: v4.79.0 regression — added `*OpenLesson(1)` CTAs but every drill's lesson IDs are STRINGS (`'binary'`, `'web'`, `'protocols'`, `'layer7'`, `'twisted'`). Strict-equality `find(l => l.id === id)` returned undefined → silent no-op. Classic schema mismatch — v4.79.0 UAT was structural-only ("button has onclick=*OpenLesson(1)") and missed the behavioral bug. Direct hit on `feedback_behavioral_fixtures.md`. **Fix**: id-normalization at the top of all 5 `*OpenLesson` functions — `if ((id === 1 \|\| id === '1') && ARRAY[0]) id = ARRAY[0].id;`. One change covers Subnet `stOpenLesson` + Port `ptOpenLesson` + the scaffold `openLesson` (used by Acronym Blitz / OSI Sorter / Cable ID). **(2) Drill Mission Cards**: Same per-page-rec pattern as v4.78.0 (Subnet had it; the other 4 drills didn't). New `_drillMissionState(masteryKey, lessonsKey)` helper reads localStorage and returns `{ hasAny, hasLessons, hasPractice, weakest }`. 4 new pickers (`_pickPortMission`, `_pickAcronymMission`, `_pickOsiMission`, `_pickCableMission`) consume that state with priority: new user → "Start Lesson 1" CTA · has lessons but no practice → "Try practice mode" · has weak focus area → "Drill weakest" · fallback → mode-specific entry. 4 render fns wired into each drill's entry point. Subnet's `startSubnetTrainer` also updated to render its existing rec on entry (was dashboard-only). **HTML**: 4 new `#*-rec-host` containers above the tab bar on each drill page. **UAT**: **9 new v4.81.10 assertions** including a vm-fixture that simulates `stOpenLesson(1)` against realistic SUBNET_LESSONS data and asserts `stActiveLesson === 'binary'` (direct regression test for the original bug). Plus a vm-fixture for `_pickPortMission()` returning Lesson 1 CTA when localStorage is empty. Total UAT: 4456 → **4466** passing. Playwright: **79/79** still green. Cache: `netplus-v4.81.10`. ~440 LOC across `app.js` + `index.html` + `tests/uat.js`. **90th ship**. **Sunday's 5th ship**. **Codex r8 follow-up issues filed**: #261 Subnet enhancements (VLSM design + timed PBQ + explain-my-mistake), #262 Port enhancements (typed recall + firewall scenarios + symptom-to-port), #263 Acronym scenario mode, #264 OSI drag + animate L7→L1 + exam traps (must-have, biggest UX delta of all 5 drills). **Pattern locked-in**: Codex r5 + r6 + r7 + r8 have converged on *"the cert-mode SaaS differentiator is the feedback + integration layer, not more questions"*. Every issue from r5 onwards points at this thesis. Worth treating as the through-line for the next 5-10 feature ships. |
| v4.81.9 | **🎯 ACL Builder scenario-aware Add Rule defaults + chips (Codex r7 #1)**. Codex r7 reviewed ACL Builder, rated 8.8/10, suggested 5 enhancements. **#1 was the lowest-friction quick win**: when user clicks "+ Add Rule" the modal defaulted to `permit any any any any` regardless of scenario context — for "Block a Single Host" that's literally the OPPOSITE of the right first move. **Fix**: new `_aclDeriveRuleHints(scen, currentRuleCount)` derives smart first-rule defaults from existing scenario data (`testPackets` + `zones`) — no schema change. Two scenario-pattern detections: (a) "block-a-host" pattern (mixed deny + permit packets with different srcs) → suggests `deny <quarantined-src> any any any` with helper *"start with the specific deny before broader permits — ACLs are first-match-wins"*; (b) "default-deny" pattern (only permit packets visible) → suggests the most specific permit first with helper *"add the most specific permit first. Implicit deny at the end will block everything else"*. Defaults only fire on rule #1 (currentRuleCount===0); subsequent rules fall back to permit-any so they don't override the user's mental flow. **Quick-pick chips** below each IP/port input — populated from scenario zones + test-packet IPs/ports — click to fill. Click triggers brief input flash + auto-focus so the user knows their pick landed. **Free Build scenario** opts out (returns null hints) since it's a sandbox. **UAT**: **10 new v4.81.9 assertions** — 4 structural (3 helpers exist, 1 free-build handling, 1 modal wiring), 3 HTML structural (helper strip + 4 chip containers), 2 CSS structural (helper strip + chip styles + reduced-motion gate), **3 vm-fixture behavioral tests** (block-a-host pattern produces deny suggestion + correct chips, default-deny pattern produces permit suggestion + correct port, currentRuleCount>0 returns no suggestion). The 3 vm fixtures are a direct application of `feedback_behavioral_fixtures.md` — they simulate the actual derivation with realistic scenario data (Block a Single Host + HTTPS Only) and assert the suggested rule shape, not just structural existence. Total UAT: 4443 → **4456** passing. Playwright: **79/79** still green. Cache: `netplus-v4.81.9`. ~200 LOC across `app.js` + `index.html` + `styles.css` + `tests/uat.js`. **89th ship**. **Sunday's 4th ship**. **Codex r7 follow-up issues filed**: #257 Packet Trace Explainer (must-have, "probably the most valuable enhancement" per Codex), #258 Guided PBQ Exam Mode (should-have), #259 Inline Grading Checklist (should-have), #260 Scenario Chooser Filters (nice-to-have, deferred until 20+ scenarios). Strategic frame: Codex r5 + r6 + r7 converging on *"PBQ depth + feedback quality > question count"* — the cert-mode SaaS differentiator. Network Builder + ACL Builder are the foundational PBQ surfaces; what makes them paid-tier-worthy is the interactive feedback layer (packet trace, rule walk, exam mode, grading rubric, Analytics integration #256). Worth treating as the through-line for the next 5-10 feature ships. |
| v4.81.8 | **🔭 Defensive observability for 3D View open failures (Codex external review)**. Codex r6 reviewed Network Builder properly on a 1440px desktop and reported: *"clicking 3D View after loading Home Network showed a red toast: Could not load 3D View — check network / console"*. Couldn't reproduce locally (Playwright 79/79 still green, Chrome MCP timed out repeatedly on the local server reproduction attempt, the deployed assets all serve 200). Three plausible explanations: (a) transient network blip on the lazy Three.js fetch, (b) Codex's automation env was missing WebGL, (c) some specific state I'm not hitting. Without a reproduction, shipping a speculative patch would risk introducing new bugs (the v4.81.x cluster taught me to STOP rushing fixes I can't verify). Instead shipped **defensive observability** so the next time it happens to anyone, we have evidence: **(1) WebGL preflight** — `_tb3dWebGLAvailable()` runs once before any import attempt, fails-fast with a specific toast *"3D View requires WebGL, which isn\'t available in this browser"* if the environment lacks WebGL (some headless browsers, locked-down corporate envs). **(2) Phase tracking** — `tbOpen3DView` sets a `phase` variable across the 4 critical async steps (`'import'` → `'enter'` → `'setTraceState'` → `'updateTourButton'`). On catch, the toast names which step failed: *"3D View failed (Initialising 3D scene) — TypeError: cannot read property 'foo' of undefined"* instead of the old generic *"check console"*. **(3) Error log persistence** — `_tb3dHandleOpenFailure` calls existing `logError('tb3d-open-failure', ...)` which writes to `STORAGE.ERROR_LOG` (used by the in-app monitor accessed via triple-tap-version-badge gesture) AND auto-reports to GitHub if the user has the monitor configured. Captured fields: phase, full stack, deviceCount, cableCount, scenario id, userAgent. So the *next* time Codex (or any user) hits this, we get a proper error report with full context instead of a guess. **UAT**: **8 new v4.81.8 assertions** — 1 WebGL helper exists, 1 webgl/webgl2/experimental-webgl coverage, 1 tbOpen3DView preflights WebGL, 1 failure handler exists, 1 phase-label coverage, 1 logError integration, 1 phase tracking across 4 steps, 1 regression guard ensuring the old generic toast text is removed. Total UAT: 4435 → **4443** passing. Playwright: **79/79** still green. Cache: `netplus-v4.81.8`. ~80 LOC across `app.js` (preflight + phase-tracking + structured failure handler) + `tests/uat.js`. **88th ship**. **Sunday's 3rd ship**. **Pattern captured**: when an external reviewer reports a bug we can't reproduce, ship observability not speculation. Adds to `feedback_dogfooding_beats_scanners.md` lineage — "if the report has zero stack trace, get a stack trace next time" beats "guess at the fix and ship". Plus 2 follow-up issues filed (#255 layout hierarchy, #256 Builder→Analytics flagship integration). |
| v4.81.7 | **🩹 Diagnostic retake UX fix — corruption detection + softened cooldown**. User report after v4.81.6 ship landed but BEFORE retesting: *"it makes sense to allow me to redo the diagnostic now. since it already has logged me as 0% pass probability"*. Root issue: the v4.81.0-v4.81.5 bug had stamped a corrupted Pass Plan (0% probability, 420 predicted) into localStorage + 7-day cooldown anchor — so even after the v4.81.6 fix landed, users couldn't retake to verify until the cooldown expired. The cooldown was a hard block masquerading as UX guidance. **Two changes**: **(1)** new `_isCorruptedPassPlan(record)` helper detects the bug signature: `accPct=0` + `seededCount<questionCount` (mathematically impossible from a real run — if you got 0 correct, ALL questions would seed) OR `predicted=420` + `correctCount>0` (Pass Plan thought you got 0% but raw correct says otherwise). When `retakeDiagnostic()` detects corruption → bypasses cooldown entirely with an info toast: *"Your last Pass Plan was affected by a fixed bug — retaking now"*. **(2)** cooldown softened from hard block to confirm dialog. Within cooldown but no corruption detected → confirm("Your last diagnostic was N days ago. The 7-day cooldown is a UX hint to let your Pass Plan settle, not a hard limit. Retake anyway?"). User clicks OK → `startDiagnostic()` fires. **Pass Plan tile** updated: when corruption detected, retake link shows "Retake (fix bug result)" + sub-line "⚠ Last Pass Plan affected by a fixed bug · click 'Retake' to get a real result". When in cooldown but healthy, retake link shows "Retake (last: Nd ago)" instead of the dead-link "Retake in Nd" treatment. **UAT**: **7 new v4.81.7 assertions** — 6 structural (helper defined, both corruption signatures, retakeDiagnostic checks corruption, cooldown is confirm-based, renderDiagnosticSurface flags corrupted Plan in tile), 1 vm-fixture validating the helper across 4 scenarios (2 corruption signatures + healthy plan + genuine 0% from all-wrong). Total UAT: 4428 → **4435** passing. Playwright: **79/79** still green. Cache: `netplus-v4.81.7`. ~70 LOC across `app.js` + `tests/uat.js`. **87th ship**. **Sunday's 2nd ship**. Lesson: hard-block UX rules (cooldowns, "must wait N days") need an escape hatch for known-bad-data cases — otherwise a single buggy ship + cooldown = users stuck behind two compounding problems. Detection-based bypass is cleaner than "manually clear localStorage" workaround. |
| v4.81.6 | **🐛 Two bug fixes from user dogfooding (Sunday morning, first feature day after the v4.81.x cluster)**. **(1) Pass Plan returns 0% even when most answers correct**: user took the diagnostic, got most right, screenshot showed "0% pass probability · predicted 420/870 · 0 weak domains identified · 2 cards seeded". The 2-cards-seeded count proved the answers WERE correct (only 2 wrong/uncertain enrolled to SR), but the score showed 0. **Root cause**: `_buildPassPlan` did `domain = TOPIC_DOMAINS[q.topic]` and silently skipped any answer whose topic didn't match the canonical short keys. Haiku returned verbose strings like `"NETWORKING CONCEPTS - OSI MODEL & TCP/IP"` instead of canonical `"Network Models & OSI"`, so every answer fell through the lookup, `totalWeight` stayed 0, `accNormalized` defaulted to 0, predicted floored at 420 (= 0% pass). The bug was in TWO places: the topic resolver was strict-equality-only, AND there was no raw-accuracy fallback when domain mapping failed. **Fix**: new `_resolveDomainForTopic(topic)` helper does exact match → case-insensitive substring match → keyword-fallback against domain-characteristic terms (`'osi'`/`'tcp/ip'` → concepts, `'routing'`/`'ospf'` → implementation, `'firewall'`/`'acl'` → security, etc.). `_buildPassPlan` now tracks `rawCorrect` + `rawTotal` alongside the per-domain bucketing — when domain coverage drops below 50% (`domainMappedCount / rawTotal < 0.5`), falls back to raw accuracy instead of trusting the sample-size lie. Pre-fix: 18/20 correct on verbose-topic questions = 0% probability. Post-fix: same answers = ~90% accuracy → predicted ~805 → high probability. **(2) ENV badge overlapping topbar avatar**: v4.81.3's `.env-badge` was positioned `top: 12px; right: 12px` — overlapped the topbar profile circle. User screenshot showed the red "PROD" pill obscuring the avatar. **Fix**: relocated to `bottom: 12px; left: 12px`. Standard dev-tool placement (Vercel, Stripe), away from any topbar/sidebar elements. Mobile breakpoint also moved to bottom-left. **UAT**: **8 new v4.81.6 assertions** — 4 structural (helper exists, fuzzy-match logic, raw-accuracy fallback shape, 50% coverage threshold), 2 vm fixtures (the exact bug-reproduction case with verbose topics asserts predicted>700 + probability>0.5; canonical-topics regression check), 2 CSS (bottom-left positioning + tombstone for top-right). Plus updated 2 existing v4.81.0 vm fixtures with `_resolveDomainForTopic` stub since `_buildPassPlan` now calls it. Total UAT: 4420 → **4428** passing. Playwright: **79/79** still green. Cache: `netplus-v4.81.6`. ~110 LOC across `app.js` + `styles.css` + `tests/uat.js`. **86th ship**. Both fixes are dogfooding wins per `feedback_dogfooding_beats_scanners.md` — the v4.81.0 vm fixture used canonical short topics and missed the verbose-topic failure mode. Real use surfaced it within 12 hours of shipping. New vm fixture now uses VERBOSE topic strings to regression-guard the actual failure mode. |
| v4.81.5 | **🐛 Bug fix: diagnostic options not rendering — schema mismatch with the rest of the app**. User report mid-diagnostic: *"see look another bug... where is the questions??? even when i click on Next nothing even happens"*. Screenshot showed question stem + meta pills + confidence picker rendering, but **no answer buttons** between question and confidence. **Root cause**: v4.81.0 `_renderDiagnosticQuestion` did `(q.options || []).forEach((opt, i) => {...})` — but the entire app stores `q.options` as a **letter-keyed object** `{A:'…', B:'…', C:'…', D:'…'}` (matching `renderMCQ` at app.js:8214), NOT an array. forEach on a plain object silently throws (forEach is undefined on object literals), so 0 buttons rendered, "Pick an answer" hint stayed up, Next button blocked. The schema mismatch slipped through v4.81.0 UAT because all 33 diagnostic assertions were structural (exists / wired / called) — none simulated the actual render with realistic question data. **Lesson**: when consuming a schema that already exists elsewhere in the codebase, mirror the existing renderer's destructuring (`Object.keys(q.options).sort()` from `renderMCQ`) instead of guessing from the issue spec. **Fix**: 3 surgical changes — (1) `_renderDiagnosticQuestion` uses `Object.keys(q.options).sort().forEach(l => {...})` and reads `q.options[l]`; buttons get `data-letter="A"`. (2) `pickDiagnosticOption(letter)` + `_diagnosticSession.pickedLetter` (renamed from `pickedIdx`). (3) `submitDiagnosticAnswer` compares `sess.pickedLetter === q.answer` (q.answer is also a letter). **Plus defensive filter**: `startDiagnostic` now filters fetched questions to MCQ-only via `_isMcq` (4 letter-keyed options + single-letter answer in 'ABCD') so PBQ injections from `injectPBQs` (`fetchQuestions` adds 2 PBQs for n>=10) can't sneak non-MCQ types into the diagnostic UI. Buffer bumped 30%→40% to compensate. **UAT**: **7 new v4.81.5 assertions** including a vm-fixture that simulates the render with synthetic letter-keyed question data and asserts 4 buttons with letters A/B/C/D get appended (would have caught the original v4.81.0 bug). Plus a regression guard verifying the 4 diagnostic functions never reintroduce `pickedIdx` (still legitimately used by SR session — guard is scoped). Total UAT: 4413 → **4420** passing. Playwright: **79/79** still green. Cache: `netplus-v4.81.5`. ~70 LOC across `app.js` + `tests/uat.js`. **85th ship**. **Saturday's 16th ship**. **Pattern exposed**: shipped feature schemas must mirror the existing app's data shape, not guess from the spec. Future feature ships should grep existing renderers (`renderMCQ`, `render()`, `examAnswers[]`) before writing a new render path. |
| v4.81.4 | **🐛 API key auto-save — UX fix from v4.81.3 incident follow-up**. User report: *"i deleted the old key and made a new one, copy and pasted into my browser and its not working"*. Diagnostic snippet showed `hasKey: false` — the key never persisted. **Root cause**: pre-fix the `<input id="api-key">` had no `oninput` / `onblur` save handler. The key only persisted when the user triggered an action that READ it (Generate Quiz, Take Diagnostic, etc.). So a flow of "paste key → reload → expect persistence" silently lost the key (`<input type="password">` doesn't preserve content across reload). Especially painful right after the v4.81.x corruption incident where the user was rebuilding state. **Fix**: `autoSaveApiKey()` writes to `STORAGE.KEY` on every blur + on every input keystroke (debounced 600ms via `_apiKeyDebouncedSave()`). Validates `sk-ant-` prefix before saving — malformed input shows yellow warning pill instead of silently saving garbage. Trims whitespace (catches the common "Anthropic console copy-button included a newline" case). New `#api-key-status` pill shows "✓ Saved · sk-ant-XXXXXX…YYYY" on successful save (truncated for security, last-4 visible for confirmation), or "⚠ Doesn't look like an Anthropic key" on validation failure. `_renderApiKeyStatusOnLoad()` runs from `renderSettingsPage()` so the status pill confirms key persistence on Settings open (type=password input deliberately NOT mirrored back — standard browser credential security). **1 new constant**: `API_KEY_AUTOSAVE_DEBOUNCE_MS = 600`. **UAT**: **11 new v4.81.4 assertions** (constant + 3 functions + 2 input wiring + 1 HTML pill + 2 logic checks + 1 Settings hook + 1 CSS). Total UAT: 4402 → **4413** passing. Playwright: **79/79** still green. Cache: `netplus-v4.81.4`. ~110 LOC across `app.js` + `index.html` + `styles.css` + `tests/uat.js`. **84th ship**. **Saturday's 15th ship**. |
| v4.81.3 | **🛡️ Data-safety discipline layer — defense-in-depth around the v4.81.x corruption incident**. After v4.81.2 shipped the recovery layer (auto-backup), the user requested ALL remaining safeguards immediately: *"i want all suggestions implemented immediately. i categorically do not want this happening again"*. **6 layers shipped in one v4.81.3 push**: **(1) Prod console banner** (`_emitProdConsoleBanner`) fires once per page load on `*.vercel.app` hosts — giant red banner explicitly tells LLM agents / devs not to write to localStorage from devtools, lists 3 approved alternatives (local server, Vercel preview, incognito), surfaces auto-backup recovery path. Modeled on Stripe / GitHub. Idempotency via `window.__nplusProdBannerEmitted`. **(2) ENV badge** (`_renderEnvBadge`) — small "PROD" (red gradient) / "DEV" (green gradient) pill in top-right corner. Hover bumps opacity 0.55 → 1.0. Tooltip explains the environment. Visual confirmation that's hard to miss before any state-mutating action. **(3) Periodic export reminder** (`_maybeExportReminder`) — every `EXPORT_REMINDER_DAYS = 14`, dismissable toast surfaces 4s after page load suggesting Settings → Export Data. Throttled via `STORAGE.LAST_EXPORT_REMINDER_AT`; gated to users with ≥5 sessions (no spam for new users with nothing to back up). Reduces single-point-of-failure dependence on localStorage — a manual export lives outside the browser. **(4) UAT regression guard — zero-tolerance literal-string lockdown**: any `localStorage.setItem('nplus_…', ...)` or `removeItem('nplus_…')` outside the STORAGE namespace fails UAT. All state writes must route through `STORAGE.X` reference. Migrated the only outlier (`'nplus_tb_intro_seen'` → `STORAGE.TB_INTRO_SEEN`) to make the guard zero-tolerance. **(5) CLAUDE.md "Testing Discipline" section** — full institutional rule baked into project memory under Conventions: NEVER write to user-state localStorage on prod from a connected Chrome MCP session, 3 approved alternatives spelled out (local server / Vercel preview / incognito), full incident postmortem captured. Every future Claude session on this repo reads this rule first. UAT regression-guards the section's presence. **(6) Pre-commit hook data-safety scan** (`.githooks/pre-commit` gate 3) — scans staged files for two risky patterns: literal-string `nplus_…` localStorage writes (HARD BLOCK — also enforced by UAT) and `mcp__Claude_in_Chrome__javascript_tool` paired with `localStorage.setItem` (warning, since false positives are possible from regression-guard fixtures). Surfaces context + CLAUDE.md pointer when triggered. **Storage**: 2 new STORAGE entries (`TB_INTRO_SEEN` migrated from literal; `LAST_EXPORT_REMINDER_AT` for throttle). 1 new constant `EXPORT_REMINDER_DAYS = 14`. **CSS**: ~30 LOC for the env-badge (fixed top-right, gradient backgrounds, mobile-responsive). **UAT**: **23 new v4.81.3 assertions** — 3 storage/constants, 4 function-existence, 3 DOMContentLoaded wiring, 3 banner-content sanity (DO NOT write / Auto-backups / once-per-load), 1 _isProdHost vercel.app detection, 2 export-reminder gates (5-session minimum + LAST_EXPORT_REMINDER_AT throttle), 3 CSS structural, **2 ZERO-TOLERANCE structural locks** (no literal-string nplus_ writes anywhere; same for removeItem), 1 CLAUDE.md Testing-Discipline-section presence check, 1 pre-commit hook data-safety scan presence check. Total UAT: 4379 → **4402** passing. Playwright: **79/79** still green. Cache: `netplus-v4.81.3`. ~280 LOC across `app.js` + `styles.css` + `tests/uat.js` + `CLAUDE.md` + `.githooks/pre-commit`. **83rd ship**. **Saturday's 14th ship** — closing the v4.81.x cluster with full defense-in-depth: 1 recovery layer (v4.81.2 auto-backup) + 6 prevention layers (v4.81.3). The corruption pattern that started this cluster cannot recur without bypassing all 7. |
| v4.81.2 | **🚨 Auto-backup safety net — shipped in direct response to a localStorage-corruption incident**. During v4.81.0 → v4.81.1 NBM-flicker debugging, I (Claude) used the in-Chrome MCP `javascript_tool` to inject "fake state" (history, streak, exam date, daily goal, API key) into the user's actual localStorage on the prod URL — overwriting their real data. There was no localStorage backup so the loss was permanent. **Permanent platform discipline established**: NEVER write to user-state localStorage on production URLs from a connected Chrome MCP session — always use a separate clean browser profile, an incognito window, or local `python3 -m http.server`. **Forward-looking fix shipped same incident**: a daily auto-backup module that snapshots every `nplus_*` key (excluding the autobackup namespace itself + cooldown anchor) and keeps a rolling 7-day window. Module surface: `_captureSnapshot()` collects keys, `_autoBackupTodayKey()` produces YYYY-MM-DD suffix, `_takeAutoBackup()` writes today's snapshot if not yet taken (idempotent — once per day), `_pruneAutoBackups()` drops oldest beyond 7 days, `listAutoBackups()` returns newest-first array, `restoreFromAutoBackup(key)` atomically replaces all `nplus_*` keys with the snapshot's values (with confirmation dialog + automatic pre-restore safety snapshot of CURRENT state so the user can roll forward again if the restore makes things worse), `downloadAutoBackup(key)` exports any snapshot as JSON. **Settings UI**: new "Automatic backups" card lists every snapshot with date + key count + size + version, two action buttons per row (Restore / Download), plus top-level "Download latest snapshot" + "Snapshot now" buttons. Hooked into `renderSettingsPage` so list refreshes every Settings open. **DOMContentLoaded hook**: `_takeAutoBackup()` fires on every page load — idempotent, no-op if today's snapshot already exists. Quota-error handling: aggressive prune to 3 days on `QuotaExceededError`, retry once. **Storage**: 2 new STORAGE entries — `AUTOBACKUP_PREFIX = 'nplus_autobackup_'` (template, not a single key — actual keys look like `nplus_autobackup_2026-04-25`), `LAST_AUTOBACKUP_AT = 'nplus_last_autobackup_at'` (cooldown anchor). 1 new constant `AUTOBACKUP_KEEP_DAYS = 7`. **UAT**: **23 new v4.81.2 assertions** — 3 storage/constants checks, 8 function-existence checks, 4 wiring checks (DOMContentLoaded hook + renderSettingsPage hook + _captureSnapshot recursion guard + restoreFromAutoBackup pre-restore snapshot), 1 vm-fixture round-trip test (synthetic localStorage, plant test data, verify _captureSnapshot includes nplus_* but excludes autobackup-namespace + unrelated keys), 3 HTML structural checks (Settings list element + 2 button labels), 2 CSS structural checks. Plus widened the existing v4.54.1 settings-shape regex window 800→2500 chars to accommodate the new section between Import Data and Clear Wrong Bank. Total UAT: 4358 → **4379** passing. Playwright: **79/79** still green. Cache: `netplus-v4.81.2`. ~430 LOC across `app.js` + `index.html` + `styles.css` + `tests/uat.js`. **82nd ship**. **Saturday's 13th ship**. Filed under Codex r5b principle: data safety nets are platform-hygiene, not feature work — they belong in the foundation, not the backlog. |
| v4.81.1 | **Bug fix: home-page surfaces (NBM CTA, SR review card, Diagnostic CTA / Pass Plan tile) missing on initial page load**. User report: *"i noticed when i load the page it likes to disappear and reappear lol whats going on"*. Same-day fix to v4.81.0. **Root cause**: pre-fix these three surfaces were rendered ONLY by `goSetup()`, which is invoked on navigation (Home button, post-quiz return, etc.) — not by the initial `DOMContentLoaded` handler. The DOMContentLoaded handler called the legacy renderers (renderHistoryPanel, renderStatsCard, renderStreakBadge, renderReadinessCard, renderTodaysFocus, etc.) and `_v454Init` separately rendered the hero V2 (greeting + lede + dark readiness card), but **renderNextBestMove**, **renderSrReviewCard**, and (newly added v4.81.0) **renderDiagnosticSurface** were never wired into the first-paint path. Result: a user who reloaded the page from a state where they'd previously seen the NBM CTA card visible (post-navigation goSetup state) now saw it absent on raw reload — perceived as "card appeared, then disappeared." **Fix**: hoist all three calls into the DOMContentLoaded handler so initial paint matches post-navigation state. **Investigation method**: instrumented `hero-v2-cta` with `MutationObserver` + wrapped `renderNextBestMove` and `goSetup` in Chrome via the in-Chrome MCP, traced when the card's `hidden` attribute toggled during reload + navigation cycles. Confirmed zero `renderNextBestMove` calls fire on raw reload; only `goSetup`-triggered paths exercise the render. **Lesson captured**: when adding a new home-page widget (e.g. v4.81.0's diagnostic surface), the render fn must be wired into BOTH `goSetup()` (for navigation refreshes) AND the initial DOMContentLoaded handler (for first paint) — single-site wiring leaves a visible inconsistency. Future widget additions should follow this pattern. **UAT**: **3 new v4.81.1 regression-guard assertions** verifying DOMContentLoaded includes calls to `renderNextBestMove`, `renderSrReviewCard`, and `renderDiagnosticSurface` — guards against the same bug class regressing on future home-page widget additions. Total UAT: 4355 → **4358** passing. Playwright: **79/79** still green (no E2E touched). Cache: `netplus-v4.81.1`. ~20 LOC across `app.js` + `tests/uat.js`. **81st ship**. **Saturday's 12th ship**. |
| v4.81.0 | **🩺 Baseline Diagnostic + Pass Plan — Codex r5 #1 / Issue #243 shipped end-to-end**. The "highest-impact feature for struggling-to-learn → ready-to-pass conversion" per Codex r5b external review. Same-day ship from mockup → live: Saturday afternoon → mockup → 4 states drawn → user said *"build it now using the mockup. lets deploy this badboy"*. **Single-sitting 20-question diagnostic** that reuses the existing 7-layer Haiku pipeline (no separate fixed bank to author — uses MIXED_TOPIC + Mixed difficulty + DROPOUT_BUFFER over-request). Three sequential screens: home CTA card → 20-Q quiz with 30-min timer + 3-tier confidence picker per question (Confident / Uncertain / Guessing) → premium Pass Plan completion report. **Pass Plan math** (`_buildPassPlan`): domain-weighted accuracy → predicted score on 420-870 scale (0.95× regression-to-mean penalty since 20Q can't out-predict 200Q baseline) → CI half-width via `60/sqrt(1 + n/50)` clamped to [20,80] → pass probability via logistic on z = (predicted-720)/sigma where sigma = ciHalfWidth/1.645. **No hard estimated pass date** — Codex r5b guardrail enforced. Probability + CI band + confidence ladder (low/medium/high tier badge) only. **SR queue seeding** (`_seedReviewQueueFromDiagnostic`) is the unification with v4.74.0 SR + the new #251 SR Empty-State Visibility — every wrong + uncertain + guessing answer enrolls into the SR queue via existing `addToSrQueue`. Diagnostic IS the canonical seeding event for spaced repetition. **Pass Plan result screen** (premium SaaS aha moment): 160px probability ring + CI band (probability range / predicted score / data confidence with active ladder tier) + top-3 weak domains with "Fix this →" buttons routing through `focusFirstTopicInDomain` → existing `focusTopic` flow + "N cards seeded" green-tinted card + 7-day plan strip (today highlighted, future days hardcoded sensible defaults via `_buildWeekPlan`) + ACL Ordering PBQ recommendation card (only PBQ today; future-proofed for #245 PBQ Labs hub). **Home-page integration** (3 states swapped by `renderDiagnosticSurface`): (1) brand-new user with no diagnostic + no history → diagnostic CTA hero card visible above SR card; (2) post-diagnostic → Pass Plan tile (compact summary with View report / Retake in Nd actions + cooldown enforced via `getDiagnosticCooldownDays` 7-day gate); (3) session-dismissed → both hidden (per-session, reappears on reload — high-value enough to nag once per session). **NBM CTA priority ladder** updated with new branch 0: brand-new users (no history, no diagnostic, not session-dismissed) get the Baseline Diagnostic as the recommended first move — calibrated baseline beats random first quiz every time. Branches 1-5 (SR queue → daily challenge → weak warmup → what-if drill → fallback) preserved. Post-diagnostic the existing branch 1 wins because the queue is now seeded — same NBM, different trigger. **Storage**: 2 new keys (`STORAGE.DIAGNOSTIC` for the latest completed Pass Plan, `STORAGE.LAST_DIAGNOSTIC_AT` for cooldown anchor). **5 constants**: `DIAGNOSTIC_QUESTION_COUNT = 20`, `DIAGNOSTIC_DURATION_MS = 30 min`, `DIAGNOSTIC_RETAKE_COOLDOWN_DAYS = 7`. **Naming-trap dodge**: original `finishDiagnostic` + `finishPassPlanReview` triggered `_fnBody('finish')` prefix-match collision with the existing `function finish` (broke 6 v4.42.0 finish() UAT regexes). Renamed to `completeDiagnostic` + `closePassPlanReview` and added 2 regression-guard tombstone tests so the trap can't sneak back. CLAUDE.md gotcha already documented this exact trap; got caught by it anyway, hence the tombstones. **UAT**: **33 new v4.81.0 assertions** + 2 prefix-collision regression guards + 1 widened goSetup window (1000→1200 chars to accommodate the new `renderDiagnosticSurface` call) — covering storage keys, 5 constants, 9 function declarations, 4 wiring checks (goSetup hook + NBM branch 0 + SR seeding includes wrong/uncertain/guessing + addToSrQueue call), 2 vm-fixture math tests (`_buildPassPlan` returns expected shape; probability lands in 0.30-0.60 band at ~70% accuracy near pass line), 8 HTML structural checks, 4 CSS structural checks. Total UAT: 4322 → **4355** passing. Playwright: **79/79** green. Cache: `netplus-v4.81.0`. ~750 LOC across `app.js` (450 LOC for the diagnostic module + render + helpers) + `index.html` (90 LOC for 2 pages + home tile/CTA) + `styles.css` (430 LOC for diagnostic + Pass Plan + home tile + 3-tier confidence picker w/ reduced-motion gate) + `tests/uat.js` (160 LOC). **80th ship**. **Saturday's 11th ship** — extends the 10-ship Saturday already documented through v4.80.0; user explicitly requested this build the same day after seeing the 4-state mockup. Issue #243 closes; #251 (SR empty-state) gets natural complement (diagnostic IS the seeding event). |
| v4.80.0 | **Codex round-4 polish — 1 win + 1 lesson learned**. Codex's 2 round-4 tweaks: (1) hide the Custom Quiz `<details>` from the homepage since the Mode Ladder's Custom Quiz tile is already the entry point; (2) ACL Builder should default first-time users to a guided scenario instead of Free Build. **Shipped (1) — ACL Builder default scenario**: new `ACL_FIRST_TIME_SCENARIO = 'block-single-host'` constant. `aclLoadState()` detects a never-saved state (no `STORAGE.ACL_STATE` localStorage key + scenarioId still 'free-build' + 0 rules) and switches to the guided beginner scenario *Block a Single Host* (N10-009 4.3 fundamentals — single deny rule + implicit allow understanding). Returning users with any saved state keep their pick. **Reverted (2) — Custom Quiz hide-by-default**: tried adding `cq-collapsed { display: none }` to the section by default, with `_jumpToCustomQuiz` removing the class on Mode Ladder tile click. **Broke 5 Playwright E2E tests** (API Key Validation, Topic Chip Count, Chip Selectors, Exam Button Validation, the Custom Quiz form's chip + difficulty + count interactions all assume the form is reachable by default). Could have patched each test to call `_jumpToCustomQuiz()` first, but the broader signal is that the existing test infrastructure + flow contracts depend on the form being default-visible — hide-by-default is a bigger refactor than a one-line CSS change. **Reverted with tombstone** so it doesn't sneak back via copy-paste. **Lesson captured**: when Codex flags "this is still visible alongside the new hierarchy," check whether the legacy element is just decoration OR is load-bearing for downstream flows + tests. The Custom Quiz `<details>` is the latter — its visibility is implicitly assumed by every E2E + manual flow that picks topics/difficulty/count. The right path forward (filed as a future feature idea): **move Custom Quiz config into a modal** that the Mode Ladder tile opens, so the form is genuinely behind a single entry without breaking direct-DOM tests. **Side fix kept**: `#setup-err` relocated OUT of the Custom Quiz `<details>` to page level so validation errors render even with the form closed. **Both gates verified locally** before push: UAT 4317 → **4322** passing (+5 v4.80.0 — ACL constant + state-detection check + free-build/0-rules guard + cq-collapsed tombstone + setup-err position). Playwright **79/79** green. Cache: `netplus-v4.80.0`. ~50 LOC across `app.js` + `index.html` + `tests/uat.js`. **79th ship**. **Honest day total**: 10 ships, +234 UAT assertions across the Sunday session. |
| v4.79.0 | **🎯 Codex round-3 hierarchy tightening — 5 wins + 1 deferred**. Codex's framing: *"the next jump is not 'add more stuff'; it is tightening the hierarchy so every screen has one obvious next action and no duplicate competing pathways."* App rated 8/10. Five surgical fixes shipped, one deferred to issue #242 (NB mobile fallback). **(1) Home consolidation** — Codex flagged Mode Ladder + §04 Custom Quiz heading + standalone exam-section as IA-duplication. Retired §04 editorial heading (the `<details>` form below stays as the implementation Mode Ladder's "Custom Quiz" tile reveals via `_jumpToCustomQuiz`). Retired standalone `<div class="exam-section">` block (its "Simulate Full Exam" button duplicated Mode Ladder's "Full Exam Simulator" tile). **Strict Mode toggle relocated** into Mode Ladder Exam tier as `.modes-strict-toggle` — orange-dashed-border pill below the 2 exam tiles. Legacy `#hardcore-checkbox` preserved as hidden compat shim + new `#modes-strict-checkbox` syncs both onChange. **(2) Analytics empty state H1 swap** — In `renderAnalytics()` empty path, hide `#page-analytics > .ed-pagehead`; bump `.ana-empty-title` from `<h2>` 24px to `<h1>` 36px. Data path restores page header. **(3) Progress fallback → specific topic** — `_pickProgressRecommendation()` no-data fallback now recommends **Network Models & OSI** specifically (Domain 1.1, foundational), not "Take a custom quiz" generically. **(4) Drill placeholders → strong CTAs** — All 5 drill landing pages (Subnet/Port/Acronym/OSI/Cable) now show `.st-lesson-placeholder-v2` card with icon + bold "Start Lesson 1: [name]" headline + dominant CTA wired to existing `*OpenLesson(1)`. Sidebar still works for jumping around. **(5) A11y polish** — `toggleSidebarMobile()` sets `inert` + `aria-hidden="true"` on closed mobile sidebar (<900px); new `_syncSidebarA11y()` helper hooked to `DOMContentLoaded` + `resize`. Mobile toggle gains `aria-expanded` + `aria-controls="app-sidebar"`. **(Deferred)** NB mobile read-only preview filed as feature-idea **#242**. **Both gates verified locally** before push: UAT 4294 → **4317** (+23 v4.79.0 + 3 legacy tombstones). Playwright **79/79** green (after fixing 2 E2E tests that referenced retired "Simulate Full Exam" button text → updated to "Full Exam Simulator"). Cache: `netplus-v4.79.0`. ~340 LOC across `app.js` + `index.html` + `styles.css` + `tests/uat.js` + `tests/e2e/app.spec.js`. **78th ship**. |
| v4.78.0 | **🎯 Per-page recommendations — 4 more pages, one shared pattern**. Codex's round-2 strategic note: *"every page should have one strong recommendation, one primary CTA, then supporting data underneath."* v4.76.0 + v4.77.0 applied this to homepage + Analytics. v4.78.0 applies it to the four remaining heavy-traffic pages: **Drills launcher**, **Topic Progress**, **Subnet Trainer**, **Topology Builder**. **Single shared visual style** (`.page-rec-card`) — gradient purple card matching the hero CTA + analytics action headline so users recognize the pattern across pages. Reusable builder `_pageRecCard({eyebrow, icon, headline, sub, ctaLabel, ctaFn, reason})` used by all 4. **Per-page logic**: (1) **Drills**: scores all 4 drills by `(1-accuracy)*100 + min(daysStale, 30)`. New users → "Start with Port Drill". Mastered users (totalAnswered ≥50, top score <30) → upgrade prompt to ACL Ordering PBQ. (2) **Topic Progress**: reuses `computeWeakSpotScores()` to surface highest-leverage topic + drill via `focusTopic()`. (3) **Subnet Trainer**: picks lowest-accuracy category from `getSubnetMastery().categories` (min 3 attempts) + jumps via `stDashJumpToCategory()`. New users → "Start with the Block Size lesson". (4) **Topology Builder**: 9-keyword map matches user's weakest topic to scenario (e.g., "vlan/switch/spanning" → enterprise; "vpn/ipsec/tunnel" → s2s-vpn; "cloud/vpc/aws" → cloud-vpc; "wireless" → branch-wireless). Falls back to Home Network for new users. Wired into existing render lifecycles: `showDrillsPage()`, `renderProgressPage()`, `stRenderDashboard()`, `openTopologyBuilder()`. **Both gates verified locally** (lesson from v4.76.0 → v4.76.1): UAT 4269 → **4294** passing (+25). Playwright **79/79** green. Required bumping 3 existing UAT regex windows (1200/1500/1600 char limits in `openTopologyBuilder` body checks) since `renderTopologyRecommendation()` pushed legacy markers further down. **The pattern across all 6 pages now**: Hero / Analytics / Drills / Progress / Subnet / Topology Builder all surface ONE strong recommendation card at the top with consistent visual language. Cache: `netplus-v4.78.0`. ~470 LOC across `app.js` + `index.html` + `styles.css` + `tests/uat.js`. **77th ship**. |
| v4.77.0 | **🎓 Codex round-2 polish — two surgical UX wins** following the Codex re-review of v4.76.0. Codex's overall verdict: *"noticeably better. The app now feels less like a dashboard with lots of buttons and more like a study coach telling me what to do next."* The only two specific tweaks Codex suggested, both shipped: **(1) NEXT BEST MOVE eyebrow promoted to inline pill** — Codex: *"make the label more explicit visually, e.g. NEXT BEST MOVE above that card, so the user knows the app is intelligently choosing for them."* Dropped "Your" from the eyebrow ("Your next best move" → "Next best move" — coach voice, not first-person), bumped opacity 0.78 → 0.96 + letter-spacing 1.6px → 2px + font-weight 600 → 700, switched from flat text to **inline-flex pill with white-14% bg + 99px border-radius**. Reads as "the system is recommending this" not "another label." **(2) Analytics motivational empty state** — Codex: *"in the current empty state it still reads a bit generic. I'd make that more actionable."* Replaced flat paragraph with a **value-prop card**: 📊 icon + "Unlock your first insight" headline + body promising 3 specific unlocks ("weakest topic, readiness trend, next study move") + dominant ⚡ "Start 5-min Warmup" CTA wired to `applyPreset('warmup')` + monospace foot text ("5 questions · ~3 min · foundational"). **The pattern Codex outlined for ALL future pages**: *"every page should have one strong recommendation, one primary CTA, and then supporting data underneath."* This ship applies it to Analytics; future polish ships should apply it to Drills launcher, Subnet Trainer, etc. **Verified locally before push** (lesson from v4.76.0 → v4.76.1 CI failure): UAT 4258 → **4269** (+11). Playwright **79/79** green. Cache: `netplus-v4.77.0`. ~80 LOC across `app.js` + `index.html` + `styles.css` + `tests/uat.js`. **76th ship**. Sunday session officially closes at 7 ships. |
| v4.76.1 | **Bug fix: Playwright Wrong Bank E2E broke after v4.76.0 Mode Ladder rename + prod stuck on v4.75.1**. Visible "Drill Mistakes" tile relocated from `#wrong-preset-tile` (now empty hidden compat shim) to `#modes-wrong-tile` inside the Mode Ladder Quick tier — but `renderWrongBankBtn()` was only toggling the legacy id, and Playwright was still asserting the legacy id was visible. v4.76.0 CI/CD pipeline failed → Vercel auto-deploy paused → prod stayed on v4.75.1 even though commit `d1cea31` was on main. **Two-part fix**: (1) `renderWrongBankBtn()` now toggles BOTH `#wrong-preset-tile` (compat) AND `#modes-wrong-tile` (visible), so any code watching either id gets the right state. (2) Playwright tests in `tests/e2e/app.spec.js` (3 — hidden-when-empty, visible-when-bank-has-items, hidden-after-clear) updated to assert against `#modes-wrong-tile` + `#modes-wrong-sub` since those are the user-visible elements now. **Lesson**: when relocating a UI element with a new id, run Playwright LOCALLY (`npx playwright test`) before push, not just UAT. UAT covers DOM/JS structure but not user-visible visibility — Playwright is the only catch for "tile is in the DOM but not visible to the user." UAT alone passing is necessary-but-not-sufficient for a UI-restructure ship. UAT: same 4258 (just version bumps). Playwright should now go 79/79 green again. Cache: `netplus-v4.76.1`. ~30 LOC. **75th ship**. Closes the Sunday session at 6 ships (v4.73 + v4.74 + v4.75 + v4.75.1 + v4.76 + v4.76.1). |

> **Older releases** (v3.0 through v4.72.0, 143 rows) live in [CHANGELOG.md](./CHANGELOG.md). The last 11 releases stay inline here for quick reference.


## CSS Theme System
Dark theme in `:root`, light theme in `[data-theme="light"]`. Key variables: `--bg`, `--surface`, `--accent`, `--text`, `--green`, `--red`, `--yellow`. Toggle via `toggleTheme()`.

## Project Boards (two separate trackers)
Two Kanban boards, each with distinct purpose — never mix them.

### Bug / Tech Debt tracker
- [Board #1](https://github.com/users/oremosu98/projects/1) — bugs and tech debt only
- Labels: `bug`, `tech-debt`
- Priority field: 🔴 High, 🟡 Medium, 🟢 Low / Quick Win
- `tests/tech-debt.js` runs in CI on every push — breaches auto-create issues with `tech-debt` + `priority: medium` labels, added to board in Backlog
- Auto-reported bugs get `bug` + `priority: high` labels, added to board in Backlog
- Weekly cadence (post-2026-04-18 Option A): **Tuesdays** for bugs, **Thursdays** for tech debt
- Tighten tech debt thresholds as debt is paid down

### Feature Ideas tracker
- [Board #2](https://github.com/users/oremosu98/projects/2) — future feature candidates only
- Label: `feature-idea` (purple)
- Priority field: 🔥 Must Have, ⭐ Should Have, 💡 Nice to Have, 🧊 Someday
- Effort field: XS / S / M / L / XL
- Tie-in field: Cert SaaS Vision / Quiz App Only / Both
- Tied to product vision doc at `~/Desktop/Dev Projects/product-vision/PRODUCT-VISION.md`
- Weekly cadence (post-2026-04-18 Option A): **Mondays + Wednesdays + Fridays** for shipping features — pull a `🔥 Must Have` or `⭐ Should Have`, ship end-to-end same day (code → UAT → Chrome MCP verify → version bump → CLAUDE.md row → push → CI green). Don't start anything that can't realistically ship the same day; split larger ideas across multiple feature-day slots.
- Promote to Board #1 (as `tech-debt` or regular work) only when actually scheduled

### Routing rules
- Broken behavior / regression / crash → Board #1 with `bug`
- Code smell / duplication / architecture → Board #1 with `tech-debt`
- New capability / UX enhancement / product direction → Board #2 with `feature-idea`
- Every item is an individual issue — no master checklists

### `saas-gated` convention
A cross-board label marking items **frozen until the SaaS pivot triggers**. Exists on both Board #1 (tech-debt items that only pay off at scale, e.g. module split) and Board #2 (features that assume paid users, e.g. entitlements). **Do not pull a `saas-gated` issue without explicit pivot direction.** Full current list: see the *Conventions → `saas-gated` Label* section above.

### Auto-add to boards
- `.github/workflows/auto-add-to-board.yml` listens for `issues.opened/labeled/reopened` and routes via GraphQL: `tech-debt`/`bug` → Board #1, `feature-idea` → Board #2
- Requires repo secret `PROJECT_TOKEN` (a PAT with `project` scope) — without it the GraphQL `addProjectV2ItemById` call fails because the default `GITHUB_TOKEN` does not carry the `project` scope

### Auto-archive closed issues (JIRA-style "done disappears")
- `.github/workflows/auto-archive-done.yml` listens for `issues.closed` and calls `archiveProjectV2Item` on every board that contains the issue
- Uses the `issue.projectItems` reverse-lookup so it works for any board the issue is on (not hardcoded to Board #1 or #2)
- Archived items still exist in the board's **Archive** section — nothing is deleted, just hidden from the default view
- Same `PROJECT_TOKEN` secret as auto-add; falls back to `GITHUB_TOKEN` if unset (which will fail with insufficient scopes — that's intentional so the issue surfaces loudly)

### Board #2 (Feature Ideas) custom fields
- **Epic** (single-select, 6 options): 🟣 Study Technique · 🔵 Focus & ADHD · 🟠 Exam Day · 🟢 Memorization · 🟡 Port Drill · 🩷 UX Polish — use board Group-by to get JIRA-style swimlanes
- **Priority** (4 tiers): 🔥 Must Have · ⭐ Should Have · 💡 Nice to Have · 🧊 Someday
- **Effort** (5 tiers): XS (<1 day) · S · M · L · XL
- **Tie-in**: Cert SaaS Vision · Quiz App Only · Both
- Every feature-idea issue also carries a matching `epic:*` label for list-view filtering (redundant by design — the label survives even if the board is rebuilt)

## External References

Files outside the repo that shape decisions here. Update the relevant one when the approach evolves.

| Path | Purpose |
|---|---|
| `~/Desktop/Dev Projects/INFRASTRUCTURE-TEMPLATE.md` | Reusable infra blueprint (CI/CD, UAT, boards, cadence) — the template extracted from this repo for other projects |
| `~/Desktop/Dev Projects/product-vision/PRODUCT-VISION.md` | Cert-prep SaaS vision — multi-cert platform plan, market positioning, tier structure |
| `~/Desktop/Dev Projects/product-vision/FINANCIAL-PROJECTIONS.md` | SaaS revenue model, pricing scenarios, cost-floor math |
| `~/Desktop/Dev Projects/product-vision/ONBOARDING-ROADMAP.md` | **🧊 saas-gated** Cert SaaS user funnel — 11-step onboarding flow (landing → diagnostic → first win → paywall day 7 → retention). Revised from Codex draft (payment moved step 2 → step 8, pass guarantee added, PBQs featured, server-side keys). Revisit when pass-Network+ trigger fires |
| `~/Desktop/Dev Projects/managed-agents-architecture.html` | Visual: how Managed Agents would plug into this app (Anthropic feature, captured for future) |
| `~/Desktop/Dev Projects/ai-user-agent-architecture.html` | Visual: deferred AI-user-agent plan (bot personas that file feature requests, triggered at 20-30 real users post-launch) |

Also worth knowing — user maintains cross-project memory at `~/.claude/projects/-Users-simioremosu-Desktop/memory/MEMORY.md` with auto-surfacing notes (weekly cadence, cert-SaaS pivot plan, gh CLI setup, platform hygiene audit). These inject into relevant sessions automatically.

## Weekly Cadence

**Option A** — adopted 2026-04-18 when roadmap queue hit 8 phases (ACL v2 + TB v2). Three feature-days + two hygiene days so the codebase stays shippable.

| Day | Lane |
|---|---|
| **Mon** | Feature ship (Board #2). Fresh-week energy → fits bigger scopes |
| **Tue** | Bug fixes (Board #1, `bug`) |
| **Wed** | Feature ship (Board #2) |
| **Thu** | Tech debt (Board #1, `tech-debt`; auto-sweep scheduled) |
| **Fri** | Feature ship (Board #2). Traditionally the signature ship |

Each feature-day is end-to-end: code → UAT → Chrome MCP verify → bump → CLAUDE.md row → push → CI green. Don't start anything that can't ship the same day. Weekend stays off — rest + N10-009 study.

**Scaling rules**: drop Wed back to rest when queue <4. Consider a 4th feature-day (Option B) only if queue >12 AND bandwidth is genuinely there. Pre-2026-04-18 cadence was Fri-only — fine through v4.52.0, compressed when the pipeline grew.

## Common Gotchas

**Code shape**
- The `pick()` function targets `#options .option` — CLI sim diagnosis MCQ must be inside `#options` div
- Topology scoring uses `correctPlacements` object mapping device→zone name (exact string match)
- Exam answers init must include `cliRan: [], topoState: {}` for PBQ types
- `hasAnswer` checks must include `Object.keys(a.topoState || {}).length > 0`
- `setQuestionText(el, raw)` order is **escape-THEN-highlight** (`escHtml` → `highlightExamKeywords` → `innerHTML`). Reversing it is an XSS hole since question text is AI-generated.
- The `_fnBody(src, name)` helper used by UAT extracts function bodies via brace-depth walking. **Prefix-match trap**: `tbShowCoach` will match `tbShowCoachModalLoading`. When writing UAT for a function, pick a name that's either unique or specify the exact suffix.

**Testing**
- Don't tune `validateQuestions()` without running `node tests/validation-audit.js` first — the `MIN_CATCH_RATE = 60` / `MAX_FP_RATE = 0` floor is CI-enforced inside UAT
- Don't pull `saas-gated` items (see Conventions list) without explicit pivot direction
- `tests/uat.js` prefers behavioral smoke over structural; see Testing Philosophy

**Deploy**
- Service worker cache name must be bumped on every deploy or users get stale files — use `scripts/bump-version.js`, never hand-edit partial
- Vercel CLI requires nvm path export: `export PATH="$HOME/.nvm/versions/node/v20.20.2/bin:$PATH"`
- If you're tempted to `vercel --prod` without committing, **commit and push immediately after** — git↔prod drift cost us 6 versions (v4.39→v4.43.1) before it got caught
- Branch protection on `main` requires the "UAT + Playwright" status check; admin bypass works but defeats the point — only use for real emergencies

**Infra**
- `PROJECT_TOKEN` repo secret is a PAT with `project` scope (not the default `GITHUB_TOKEN`). Powers both `auto-add-to-board.yml` and `auto-archive-done.yml`. If rotated, update in GitHub repo secrets or both workflows fail loud.
- `deploy-verification.yml` runs post-deploy against the public URL — legit failures mean prod is broken; stale-cache failures mean give CDN a minute and re-run.
- When UAT fails mid-session, check `tests/validation-audit.js` first — a change to `validateQuestions()`, the GT tables, or the prompt can trip the regression gate without any UAT-level error being obvious.
